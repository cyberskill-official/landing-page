# Scene 6 CTA Hub — Storyboard

t=0.0 The Scene 5 globe fades back. The DOM caption appears first: "You bring the will. We bring the real."

t=0.3 Lumi settles beside the hub in a calm facing-camera pose. The head is free to rotate, but the body remains forward.

t=0.6 Three portals arrive in order: Buy, Partner, Join. Buy uses solid gold-400 with a dark label. Partner uses brown-500 with a gold edge. Join uses gold-100 with a brown edge.

t=0.9 Each portal shows its sub-headline and describedBy copy. The 44x44 target annotation remains visible on every breakpoint.

t=1.2 Keyboard focus moves through Buy, Partner, then Join. The focused portal receives the 2px gold ring with 2px offset, and Lumi turns toward it without exceeding +/-30 degrees.

t=1.6 The deep-link variant demonstrates `?track=partner`: Partner is pre-focused on arrival and marked as the active conversion track.

t=2.0 A click opens the lazy form modal. The Suspense fallback says "Loading..." with `aria-live="polite"`, then the selected form replaces it.
