# Scene 2 Transformation — Storyboard

t=0.0 The surface resolves to brown-500. A gold-50 sketchpad plane sits inside the canvas boundary. Lumi is already in the `paint` clip, right arm extended, with the trail head starting at the hand.

t=0.5 The DOM caption beat 1 appears in JetBrains Mono: "Most software dies in the gap between sketch and ship." The line stays outside the canvas.

t=1.2 Lumi sweeps the gold-400 trail across the sketchpad. The trail is six additive segments with an alpha-fade tail and `genie_rim` glow.

t=2.0 The first wireframe marks land: header, columns, and content strokes. All marks are gold and brown only.

t=3.0 The wireframe begins its sketch -> system morph. Strokes thicken, the gold-50 plane becomes an app surface, and brown panels settle into place.

t=3.5 The testimonial slot appears as one DOM `<blockquote>` overlay, attributed and ready for the implementation copy source.

t=4.0 Caption beat 2 lands as its own mono line: "We close it." The app shell is complete and the trail glow fades to idle.
