window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-016"]="<h1>TASK-OPS-016: Optimize SVG vector assets in build/CI and enforce file-size budgets</h1>\n<h2>0. Why (evidence)</h2>\n<p>To prevent oversized, unoptimized SVG assets (like logos and decorative graphics) from regressing first paint speeds. Automating SVG minification in the build pipeline ensures that any new vector graphics committed stay optimized and compressed.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 All SVG assets stored in <code>/public</code> or <code>/components</code> SHALL be audited and minified (using SVGO or similar tools) to strip metadata and editor tags.</li><li>1.2 The build size check script (<code>check-asset-size.mjs</code>) SHALL enforce a strict file-size budget for SVG assets, throwing errors if limits are exceeded.</li><li>1.3 Pre-commit hooks or CI workflows SHALL validate that committed SVG files are optimized.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - all SVGs in the codebase have comments, editor tags, and unused namespaces stripped - test: <code>perf/svg-minified</code></li><li>[ ] AC for 1.2 - check-asset-size script throws an error if any SVG exceeds its budget - test: <code>perf/svg-budget-gate</code></li><li>[ ] AC for 1.3 - files are validated dynamically in pre-commit tasks - test: <code>perf/svg-precommit</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>SVGs containing inline scripts or complex styling rules being broken by aggressive minification.</li><li>ViewBox scaling attributes being stripped, breaking responsive resizing.</li></ul>\n<h2>4. Out of scope</h2>\n<ul><li>Optimizing non-vector image formats (handled in TASK-PERF-010).</li></ul>\n<h2>5. Protected invariants</h2>\n<ul><li>Minified SVGs must preserve all original visual designs and accessibility descriptors (like <code>role</code> or <code>aria-label</code>).</li></ul>";
