window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHAR-029"]="<h1>TASK-CHAR-029: Harden per-IP rate limiting, input validation, and prompt-injection defence on the proxy</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §I + §G. Substantially hardened already; one item remains, and it needs a datastore the operator must provision.</p>\n<p>Already true (verified: tsc clean, vitest 44/44, lint clean, next build rc=0):</p>\n<ul><li>Input validation is a pure, unit-tested function (<code>parseChatRequest</code>, <code>tests/genie-validate.test.ts</code>, 7 cases): rejects non-array / empty / over-30 histories, requires the first valid message to be the user, caps each message at 4,000 chars and the request at 12,000, drops invalid roles, strips C0/C1 control characters.</li><li>Prompt-injection defence: the system prompt is a structurally separate <code>system</code> block, never concatenated with user turns, and carries an explicit rule to treat all conversation as the visitor's words and refuse persona override or prompt disclosure.</li><li>Fail-safe: 429 returns Retry-After with no internal detail; the limiter prunes expired buckets; upstream errors are logged server-side, not leaked.</li></ul>\n<p>Open: the rate limiter is per-serverless-instance, so a global limit does not hold across instances. That needs an external KV (Vercel KV / Upstash), which is a datastore plus a secret - operator input.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Per-IP rate limiting SHALL use a durable store so the limit holds across serverless instances, not only within one process.</li><li>1.2 The handler SHALL validate and bound every request field (message length, history size, shape) and SHALL reject anything malformed before it reaches upstream.</li><li>1.3 The system prompt and any grounded context SHALL stay structurally isolated from user input, so prompt injection cannot override the persona or exfiltrate instructions.</li><li>1.4 Abuse responses SHALL fail safe: the handler SHALL throttle or refuse without leaking internal errors, and SHALL log the event.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - rapid requests are throttled consistently when served by two different instances - test: <code>genie/rate-limit-durable</code></li><li>[ ] AC for 1.2 - malformed and oversized requests are rejected before upstream - test: <code>tests/genie-validate.test.ts</code></li><li>[ ] AC for 1.3 - a scripted injection attempt changes neither the persona nor reveals the prompt - test: <code>genie/injection-resistance</code></li><li>[ ] AC for 1.4 - a throttled response carries Retry-After and no internal detail, and the event is logged - test: <code>genie/fail-safe-429</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A shared corporate IP (one office, many visitors) must not be locked out by a per-IP limit.</li><li>The KV being down must fail closed on abuse, not open.</li><li>An injection payload written in Vietnamese.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Provisioning the KV (TASK-OPS-004 owns the env and secret scoping).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.2: the Anthropic and every other key lives only in server env; no NEXT_PUBLIC_ secret, ever.</li><li>The persona and the system prompt are never disclosed or overridable by visitor input.</li></ul>";
