window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PERF-007"]="<h1>TASK-PERF-007: Eliminate the mobile hero layout shift (CLS 0.431 -&gt; below 0.1)</h1>\n<h2>0. Why (evidence)</h2>\n<p>Live mobile Lighthouse on 10 July 2026 measured CLS 0.431 on the home page against 0.002 on desktop; Google's \"good\" threshold is 0.1. It is the worst layout shift in the four-site benchmark and the single reason the mobile performance score sits at 47 while desktop scores 97 - the page is already the lightest in the set (573 KB) and its blocking time is lower than both higher-scoring rivals. Audit B estimates this one metric is worth 20-25 performance points. Named causes: the hero mascot/poster loads without reserved space, the display font swaps without a matched fallback, and the rotating placeholder in the wish input resizes its container.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The hero mascot and its static poster (public/lumi-poster.webp, the CanvasMount gate, LumiHotspot) SHALL reserve their layout box before load: explicit width/height (or an aspect-ratio box) on every viewport, so no element below them moves when the asset arrives.</li><li>1.2 The display font (Space_Grotesk via next/font in app/layout.tsx) SHALL load with a size-adjusted fallback (or <code>display: optional</code>) such that the swap produces no measurable shift in the hero H1 or the eyebrow line.</li><li>1.3 The rotating placeholder inside the wish input SHALL be pinned: its container height SHALL be fixed to the tallest rotation string so cycling placeholders cannot resize the field.</li><li>1.4 The measured mobile CLS of <code>/en</code> and <code>/vi</code> SHALL be at or below 0.1 at the 75th percentile in the Lighthouse mobile run used by CI.</li><li>1.5 The Lighthouse CI configuration (lighthouserc.json) SHALL assert <code>cumulative-layout-shift &lt;= 0.1</code> on mobile and fail the build above it.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - hero renders with reserved boxes; a mobile Lighthouse run reports zero layout-shift entries attributable to the mascot or poster - test: <code>lighthouse:mobile-cls</code></li><li>[ ] AC for 1.2 - font swap contributes no shift; the fallback metric override is present in the built CSS - test: <code>perf/font-swap-no-shift</code></li><li>[ ] AC for 1.3 - placeholder rotation does not change the input's bounding box - test: <code>wish-input/placeholder-stable</code></li><li>[ ] AC for 1.4 - mobile CLS &lt;= 0.1 on /en and /vi - test: <code>lighthouse:mobile-cls</code></li><li>[ ] AC for 1.5 - lighthouserc assertion exists and a seeded 0.2 CLS fails CI - test: <code>ci/lighthouse-assertions</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>360px and 390px widths; landscape phone; slow 3G where the poster arrives after first paint.</li><li>prefers-reduced-motion: the static poster path must also be shift-free.</li><li>Vietnamese hero copy is longer than English - the reserved boxes must hold for the VN line-count too.</li><li>No-JS: the canvas never mounts; the poster must still occupy its box.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Reducing JavaScript weight (TASK-PERF-008).</li><li>Redesigning the hero layout (TASK-A11Y-012 handles the CTA-bar overlap).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.1 HTML-first: every meaningful state stays server-rendered DOM and the canvas never owns LCP.</li><li>AGENTS.md §4.7: the CI performance budget (lighthouse/budget.json) is never relaxed to make a gate green.</li><li>AGENTS.md §4.3 progressive enhancement: the 3D scene stays code-split, gated, and ships with its static poster fallback.</li></ul>";
