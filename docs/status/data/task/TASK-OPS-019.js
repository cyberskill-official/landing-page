window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-019"]="<h1>TASK-OPS-019: Landing content read model from CyberOS (with git fallback)</h1>\n<h2>0. Why (evidence)</h2>\n<p>Publishable content today is TypeScript modules (<code>lib/content/notes.ts</code>, <code>site.ts</code>, <code>services.ts</code>). That is excellent for review and CI but blocks non-dev publishing. Operator direction: CyberOS manages blogs and related content centrally; the landing site must consume a stable read model without forking facts.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The landing app SHALL resolve notes, work/case studies, testimonials, and team data through a single <strong>content read module</strong> that returns typed records (same shapes as today's content modules where practical).</li><li>1.2 When CyberOS content is configured and reachable, the read module SHALL load <strong>published</strong> entities from CyberOS; when not configured or unreachable, it SHALL fall back to the git content modules so deploys never blank the site.</li><li>1.3 EN and VN fields SHALL ship together; a locale missing required copy SHALL fail the content gate or omit the entity (never EN-only placeholders).</li><li>1.4 The read module SHALL NOT embed a CMS editor UI in this repo.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - consumers import the read module, not ad-hoc fetch in each page - test: <code>content/read-model-shape</code></li><li>[ ] AC for 1.2 - with CyberOS down, fallback content still renders - test: <code>content/read-model-fallback</code></li><li>[ ] AC for 1.3 - bilingual parity gate covers read-model entities - test: <code>content/vi-parity</code></li><li>[ ] AC for 1.4 - no CMS admin route ships under app/ - test: <code>routes/no-cms-admin</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Stale cache after publish: document revalidation strategy (ISR or on-demand) without blocking this task's contract.</li><li>Draft entities must never appear on public routes.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Migrating every historical post (TASK-CMS-021).</li><li>Lead dual-write (TASK-OPS-020).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.5 Vietnamese-first.</li><li>Nothing published may claim a fact the company cannot evidence.</li></ul>";
