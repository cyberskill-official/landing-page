window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DS-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Style packs SHOULD layer on top of the light/dark theme so a reader can pick a visual flavor without losing their theme choice.</p>\n<ol><li>The system SHOULD switch the active style pack via a <code>data-cs-style</code> attribute on <code>&lt;html&gt;</code>, with each pack expressed as <code>--cs-*</code> overrides.</li><li>A style pack MUST compose with <code>data-theme</code> rather than replace it, so light and dark both stay valid under any pack.</li><li>Packs MUST stay within the doctrine token contract and MUST NOT reintroduce raw brand hex in components.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Setting <code>data-cs-style</code> re-skins the page while <code>data-theme</code> continues to select light or dark.</li><li>Every shipped pack resolves in both themes with no missing token.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Not yet implemented; acceptance pending build.</p>";
