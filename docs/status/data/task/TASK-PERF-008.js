window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PERF-008"]="<h1>TASK-PERF-008: Cut and defer first-load JavaScript (TBT 1,370 ms -&gt; under 300 ms)</h1>\n<h2>0. Why (evidence)</h2>\n<p>Audit B measured ~900 KB of first-party JavaScript across 19 chunks plus a 157 KB Google Tag Manager file; on a throttled phone this yields 1,370 ms total blocking time, 2.4 s of script start-up and 8.4 s of main-thread work, with ~115 KB of the JavaScript unused on load. Audit C independently measured ~0.74 MB of decoded JS across 19 script files and at least one 197 ms long task. The site is the lightest page in the benchmark, so the cost is execution, not bytes.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every non-critical animation module (GSAP/Lenis choreography, MotionExtras, DepthField, BlackHole, IntroVeil, LumiMagic, SoundCues) SHALL be dynamically imported and mounted only when its section is within, or approaching, the viewport.</li><li>1.2 Sections that carry no interactivity SHALL be server components (or islands) so they do not hydrate.</li><li>1.3 The ~115 KB of first-load JavaScript reported unused SHALL be removed or deferred; <code>npm run analyze</code> output SHALL be recorded in the task audit as before/after.</li><li>1.4 Measured mobile total blocking time on <code>/en</code> SHALL be at or below 300 ms, and mobile LCP at or below 2,500 ms.</li><li>1.5 The CI asset-size guard (scripts/check-asset-size.mjs) SHALL enforce a first-load JS ceiling and fail the build above it.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - the animation chunks are absent from the initial document's script set - test: <code>perf/initial-chunks-snapshot</code></li><li>[ ] AC for 1.2 - hydration count drops; the static sections emit no client bundle - test: <code>perf/server-component-inventory</code></li><li>[ ] AC for 1.3 - first-load JS is reduced by at least 200 KB against the 2026-07-10 baseline - test: <code>ci/asset-size-guard</code></li><li>[ ] AC for 1.4 - mobile TBT &lt;= 300 ms and LCP &lt;= 2500 ms - test: <code>lighthouse:mobile-perf</code></li><li>[ ] AC for 1.5 - a seeded oversize chunk fails CI - test: <code>ci/asset-size-guard</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Deferred modules must not break the scroll choreography's ScrollTrigger registration order.</li><li>A section entering the viewport during a fast scroll must not flash unstyled or unanimated content.</li><li>Reduced-motion users must never download the animation chunks at all.</li><li>The 3D scene chunk must remain separate and desktop-gated (TASK-SCENE-001 contract).</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Removing the Lumi scene or the art direction.</li><li>Analytics deferral (TASK-PERF-009).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.1 HTML-first: every meaningful state stays server-rendered DOM and the canvas never owns LCP.</li><li>AGENTS.md §4.7: the CI performance budget (lighthouse/budget.json) is never relaxed to make a gate green.</li><li>AGENTS.md §4.3 progressive enhancement: the 3D scene stays code-split, gated, and ships with its static poster fallback.</li></ul>";
