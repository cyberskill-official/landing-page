window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHAR-022"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The real Lumi GLB MUST replace the placeholder without changing the gate.</p>\n<ol><li>The commissioned GLB (TASK-CHAR-021) MUST be integrated through a gltfjsx- generated typed component, not loaded ad hoc.</li><li>The new component MUST replace <code>LumiPlaceholder</code> (TASK-CHAR-020) at the same mount point, behind the existing CanvasMount gate, with no API change for callers.</li><li>The GLB MUST be served from the app's own static assets and MUST be draco or meshopt compressed to keep transfer size small.</li><li>If the GLB fails to load, the scene MUST fall back to the existing placeholder or poster and MUST NOT throw.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>On a gated desktop, the real Lumi model renders in place of the placeholder.</li><li>The mount point and gate behaviour are unchanged for callers.</li><li>A forced GLB load failure falls back without breaking the page.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Drop-in path ready (2026-06-24, branch <code>auto/glb-perf-a11y</code>). Status stays <code>planned</code> until the commissioned model exists and renders, since the first acceptance bullet (\"the real Lumi model renders\") cannot be verified without it. Stephen is producing the model via Meshy.</p>\n<ul><li>Done - same mount point, gate unchanged (criterion 2): <code>GenieScene</code> renders <code>GltfLumi</code> in place of <code>LumiPlaceholder</code> behind the existing CanvasMount gate, with no caller API change. Activated by setting <code>NEXT_PUBLIC_LUMI_GLB</code>.</li><li>Done - served from app static assets (criterion 3): the model is read from <code>/models/lumi.glb</code> under <code>public/</code>; the export checklist requires Draco or meshopt compression. See <code>docs/3d/lumi-glb-integration.md</code>.</li><li>Done - safe fallback (criterion 4): <code>GlbBoundary</code> + Suspense fall back to the procedural Lumi on a failed/again load without throwing (shared with TASK-SCENE-010).</li><li>Remaining - the actual model (criterion 1 + first acceptance bullet). The loader uses drei <code>useGLTF</code> with a typed <code>{ url }</code> prop and <code>scene.clone</code>; once the GLB is dropped in, swap to a gltfjsx-generated typed component if per-node control is wanted. Build-verified with <code>NEXT_PUBLIC_LUMI_GLB</code> set (compiles, 26/26 pages prerender) so the wiring is sound ahead of the model.</li></ul>";
