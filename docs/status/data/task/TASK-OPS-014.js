window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-014"]="<h1>TASK-OPS-014: Integrate Prisma PostgreSQL client with connection pooling and secure credentials</h1>\n<h2>0. Why (evidence)</h2>\n<p>To transition from the developer-centric InMemoryAdapter to a secure, enterprise-ready relational datastore (PostgreSQL) when <code>DATABASE_URL</code> is set in production. This will enforce data durability across serverless function lifecycles and ensure PDPL compliance audits are backed by a real SQL store.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The database adapter factory SHALL initialize a PrismaClient configured for PostgreSQL connection pooling when <code>DATABASE_URL</code> is present.</li><li>1.2 The Prisma client integration SHALL configure connection timeouts and retry limits to survive transient serverless cold starts.</li><li>1.3 The production database integration SHALL run all writes inside transactions where necessary to link lead updates and transcripts atomically.</li><li>1.4 The schema migration path SHALL be managed via versioned schema.sql and validated in production deployment before connection starts.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - when DATABASE_URL is set, the Prisma adapter initializes and retrieves records - test: <code>db/prisma-initialization</code></li><li>[ ] AC for 1.2 - connection timeout errors fail safe to no-op without crashing the handler - test: <code>db/prisma-connection-fallback</code></li><li>[ ] AC for 1.3 - saving transcript linked to lead uses transactions to maintain integrity - test: <code>db/prisma-atomic-writes</code></li><li>[ ] AC for 1.4 - SQL schema matches the database model definitions exactly - test: <code>db/schema-parity</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Serverless database connections exhausting pool capacity.</li><li>Database credentials containing special characters requiring URL encoding.</li></ul>\n<h2>4. Out of scope</h2>\n<ul><li>Dynamic SQL queries or raw database executions without Prisma models.</li><li>Database provisioning or hosting setups (deferred to human operations TASK-BIZ-002).</li></ul>\n<h2>5. Protected invariants</h2>\n<ul><li>User leads and transcripts must never be exposed or logged in raw query outputs.</li></ul>";
