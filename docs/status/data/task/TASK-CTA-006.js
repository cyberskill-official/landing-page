window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CTA-006"]="<h1>TASK-CTA-006: Map form leads to the CRM via a server-side webhook</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §O. app/api/lead/route.ts already fans out to a CRM webhook URL, but there is no documented field mapping and no test that the mapping survives the hop - so a schema change on either side would break the pipeline silently. TASK-BIZ-002 provides the endpoint; TASK-OPS-010 provides the alert when the hop fails.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The route handler SHALL POST a mapped payload to the CRM webhook on every valid submission, server-side only.</li><li>1.2 A field-mapping table SHALL be recorded in the repo, defining how each form field (name, email, company, intent, message, locale, source, utm_*) maps to a CRM property, including the default owner and the lead source.</li><li>1.3 A failed CRM call SHALL be logged and SHALL NOT fail the visitor's submission (best-effort fan-out), but SHALL count toward the total-failure alert in TASK-OPS-010.</li><li>1.4 The CRM endpoint and token SHALL come from server env; no secret SHALL reach the client.</li><li>1.5 Without the CRM env vars the webhook sink SHALL be skipped (not failed), and the mapping SHALL still be unit-tested against a mock so a schema drift is caught before the endpoint exists.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - a valid lead produces a CRM record with every mapped field - test: <code>api/lead-crm-mapping</code></li><li>[ ] AC for 1.2 - the mapping table exists and the test asserts it field by field - test: <code>api/lead-crm-mapping</code></li><li>[ ] AC for 1.3 - a CRM outage returns 200 to the visitor, logs the error and counts toward the alert - test: <code>lead/total-failure-alert</code></li><li>[ ] AC for 1.4 - no CRM credential appears in the client bundle - test: <code>ci/no-public-secrets</code></li><li>[ ] AC for 1.5 - with no CRM env, the lead is accepted, no call is attempted, and the mapping test still runs against the mock - test: <code>api/lead-crm-mapping</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A CRM schema change must fail the mapping test, not silently drop a field.</li><li>A duplicate submission must not create two records.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Deploying the CRM endpoint (TASK-BIZ-002).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.2: every secret lives only in server env; no NEXT_PUBLIC_ secret, ever (NFR-SEC-001).</li><li>A submitted lead is never silently lost.</li></ul>";
