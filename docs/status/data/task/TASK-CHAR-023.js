window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHAR-023"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Lumi's motion MUST be a small, named state machine.</p>\n<ol><li>The model MUST expose named clips for idle, greeting, thinking, speaking, and point, played through a Three.js <code>AnimationMixer</code>.</li><li>Transitions between clips MUST cross-fade rather than cut, and idle MUST be the resting state.</li><li>The active clip MUST be selected from the genie store (TASK-CHAR-012) so chat state drives the animation: thinking while awaiting a reply, speaking while streaming, idle when done.</li><li>When motion is not allowed the model MUST hold a single static pose and MUST NOT advance the mixer.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Each named clip plays and cross-fades into the next without a hard cut.</li><li>Chat state changes drive the matching clip via the store.</li><li>With reduced motion, Lumi holds one static pose.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Partial (status stays planned). The placeholder Lumi now reacts to the genie store as a procedural pose machine: it leans toward the viewer and brightens when the chat opens, plays a dissolve-and-reform shimmer on open/close transitions, and varies energy by <code>status</code> (idle / thinking / speaking) - all eased so states cross-fade rather than cut, with idle the resting state, and the StaticPoster holding a single static frame on the reduced/non-WebGL path. This satisfies the intent of clauses 2-4 procedurally.</p>\n<p>What still blocks a full ship: clause 1 requires <em>named animation clips played through a Three.js <code>AnimationMixer</code></em>, which depends on the rigged GLB (TASK-CHAR-022, in turn the on-hold TASK-CHAR-021). Until the GLB lands there are no named clips to mix, so this stays planned; the procedural reactivity is the down-payment and the store wiring is already in place to drive real clips later.</p>";
