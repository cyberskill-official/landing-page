window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCENE-009"]="<h1>TASK-SCENE-009: Draw-call budget, GPU disposal, and LOD for the scene</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §K. Partially done (2026-06-24, branch auto/glb-perf-a11y):</p>\n<ul><li>GPU disposal on unmount is in place: components/canvas/GltfLumi.tsx disposes geometries and materials on unmount, and LumiPlaceholder.tsx disposes the hand-built aura ShaderMaterial that R3F does not own.</li><li>The asset/budget guard holds: <code>npm run check:assets</code> keeps the gated-desktop client JS and public assets under budget, and the poster path carries no GLB cost.</li></ul>\n<p>Open: a measured steady-state draw-call count. Distance LOD is only meaningful once a real multi-resolution GLB exists, so it is deferred to TASK-CHAR-021 (on hold). Audit C independently flags the always-on scene as the main INP risk, which TASK-PERF-012 addresses.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The scene SHALL render under 100 draw calls per frame in the steady state, measured and recorded, on both the placeholder and the GLB path.</li><li>1.2 On unmount the scene SHALL dispose geometries, materials and textures and release the WebGL context, so memory does not leak across mounts.</li><li>1.3 The asset budget SHALL hold for the gated desktop path and SHALL NOT regress the static poster path.</li><li>1.4 Distance-based level-of-detail SHALL swap to lighter meshes as the subject recedes (deferred to TASK-CHAR-021 - meaningless until the multi-resolution GLB exists).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - a measured steady-state draw-call count under 100 is recorded - test: <code>scene/draw-call-budget</code></li><li>[ ] AC for 1.2 - mounting and unmounting the scene ten times shows no GPU memory growth - test: <code>scene/dispose-no-leak</code></li><li>[ ] AC for 1.3 - check:assets stays green and the poster path carries no GLB bytes - test: <code>ci/asset-size-guard</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A low-end GPU where even the placeholder is expensive - the capability gate must send it to the poster.</li><li>A remount triggered by a theme flip must not leak the shader material.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>The LOD meshes themselves (TASK-CHAR-021, on hold).</li><li>Pausing the render loop (TASK-PERF-012).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.3 progressive enhancement: the 3D scene stays code-split, gated, and ships with its static poster fallback.</li><li>AGENTS.md §4.7: the CI performance budget (lighthouse/budget.json) is never relaxed to make a gate green.</li><li>AGENTS.md §4.1 HTML-first: every meaningful state stays server-rendered DOM and the canvas never owns LCP.</li></ul>";
