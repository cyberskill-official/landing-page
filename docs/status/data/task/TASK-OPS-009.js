window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-009"]="<h1>TASK-OPS-009: Add a Content-Security-Policy, report-only first</h1>\n<h2>0. Why (evidence)</h2>\n<p>Audit B measured the header set: HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy and Permissions-Policy are all set correctly; the one missing header is a Content-Security-Policy. Audit A lists header hardening as an enterprise procurement checklist item. next.config.ts confirms CSP is absent.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The site SHALL ship <code>Content-Security-Policy-Report-Only</code> covering script-src, style-src, img-src, font-src, connect-src, frame-ancestors and base-uri, with a nonce or hash for the two inline bootstrap scripts in app/layout.tsx.</li><li>1.2 Violations SHALL be reported to an endpoint and reviewed; once the report is clean for one release cycle, the header SHALL be switched to enforcing.</li><li>1.3 The policy SHALL permit exactly the origins the site actually uses (Vercel, the analytics endpoint, the Anthropic proxy is server-side only) and nothing else.</li><li>1.4 A CI check SHALL assert the header is present on every route.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - report-only header present with the named directives and a nonce - test: <code>headers/csp-present</code></li><li>[ ] AC for 1.2 - the reporting endpoint receives and stores a seeded violation - test: <code>headers/csp-report-endpoint</code></li><li>[ ] AC for 1.3 - no legitimate site function generates a violation in a full-route crawl - test: <code>headers/csp-clean-crawl</code></li><li>[ ] AC for 1.4 - the enforcing flip is a one-line change recorded in the task audit - test: <code>headers/csp-present</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The no-flash theme script and the intro-veil script are inline - they need a nonce, not <code>unsafe-inline</code>.</li><li>next/font self-hosts, so font-src stays same-origin.</li><li>GA4 (TASK-PERF-009) needs an allowance only if consent lets it load.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Subresource integrity.</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.2: the Anthropic key lives only in server env; no NEXT_PUBLIC_ secret, ever.</li><li>The keyless-secrets NFR (NFR-SEC-001) stays intact: no secret reaches the client.</li></ul>";
