window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-004"]="<h1>TASK-OPS-004: Env and secret management across production and preview, with separate keys</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §G. Every task that touches a lead sink, a datastore, a KV or an AI key depends on this being right, and the 2026-07-11 audits add the stakes: PDPL fines reach 5% of prior-year revenue for a cross-border transfer breach, and a leaked preview key that can write production data is exactly that class of incident. Raised to MUST.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Production and preview SHALL use separate keys for every provider, so a leaked preview key can reach neither production traffic, quota, nor production data.</li><li>1.2 <code>.env.example</code> SHALL enumerate every required variable by name with placeholder values only, so a fresh checkout knows what to set.</li><li>1.3 <code>docs/deploy/env-vars.md</code> SHALL document each variable: what it is, where it is set, which environments consume it, and what breaks when it is absent.</li><li>1.4 A CI check SHALL assert that no secret value and no <code>NEXT_PUBLIC_</code>-prefixed secret appears in the client bundle or the repository.</li><li>1.5 The code SHALL read every provider key from server env and SHALL fail closed (a named error, never a silent no-op on a write path) when a required production key is absent; provisioning the separate production and preview key VALUES is TASK-BIZ-001.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - production and preview resolve distinct keys, verified in the provider dashboards - evidence: screenshots recorded</li><li>[ ] AC for 1.2 - .env.example lists every variable the code reads, with placeholders only - test: <code>ci/env-example-parity</code></li><li>[ ] AC for 1.3 - the env doc maps each variable to its environments - test: <code>docs/env-vars</code></li><li>[ ] AC for 1.4 - a seeded secret in the client bundle fails CI - test: <code>ci/no-public-secrets</code></li><li>[ ] AC for 1.5 - with a required production key absent, the write path returns a named error and the alert fires - test: <code>ci/env-fail-closed</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A preview deployment must never email a real lead or write to the production CRM.</li><li>A variable added in code but not in .env.example must fail CI, not surprise the next deploy.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Obtaining the credentials themselves (TASK-BIZ-001).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.2: every secret lives only in server env; no NEXT_PUBLIC_ secret, ever (NFR-SEC-001).</li><li>NFR-SEC-001 keyless secrets: no secret is ever committed or exposed to the client.</li></ul>";
