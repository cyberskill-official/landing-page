window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-005"]="<h1>TASK-OPS-005: Lead and transcript datastore behind /api/lead and /api/genie</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §O. Today a lead exists as an email and a Slack ping; a chat transcript exists nowhere. Audit A's conversion review (\"form starts vs completions\") and TASK-BIZ-009's SLA ritual both need a queryable store. CyberOS is the intended system of record (TASK-BIZ-002) - this task is the repo-side persistence contract, whether the store is CyberOS or a managed database.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 A managed datastore SHALL persist every lead submission received by <code>/api/lead</code>, with its locale, source, intent and UTM context.</li><li>1.2 The same datastore SHALL persist Genie chat transcripts received by <code>/api/genie</code>, linked to a lead where one exists (TASK-CHAR-028).</li><li>1.3 The schema SHALL be version-controlled in the repo so the data model changes only through a reviewed diff.</li><li>1.4 Personal data SHALL carry a retention date and SHALL be deleted when it passes, and a deletion request SHALL remove the record (PDPL right to erasure).</li><li>1.5 Without a configured datastore the persistence layer SHALL no-op safely behind the TASK-OPS-010 alert rather than failing the visitor's request; provisioning the datastore is the operator's step.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - a submission to /api/lead is stored and retrievable with all its context - test: <code>db/lead-persist</code></li><li>[ ] AC for 1.2 - a Genie conversation is stored and linkable to its lead - test: <code>db/transcript-link</code></li><li>[ ] AC for 1.3 - the schema lives in the repo and a migration is required to change it - test: <code>db/schema-versioned</code></li><li>[ ] AC for 1.4 - a record past retention is deleted, and a deletion request removes it - test: <code>db/retention-and-erasure</code></li><li>[ ] AC for 1.5 - with no store configured, a lead is still accepted and the zero-sinks alert fires - test: <code>db/no-store-noop</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The store being down must not fail the visitor's submission - persistence is best-effort behind the alert (TASK-OPS-010).</li><li>A transcript can contain personal data the visitor typed about a third party.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Provisioning the database (operator step, via TASK-BIZ-002 or a managed DB).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.2: every secret lives only in server env; no NEXT_PUBLIC_ secret, ever (NFR-SEC-001).</li><li>A submitted lead is never silently lost.</li><li>Personal data is stored only with a lawful basis and a bounded retention period (PDPL).</li></ul>";
