window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SCENE-005"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The camera SHOULD follow an authored shot list, one shot per scene.</p>\n<ol><li>Camera motion MUST come from an authored keyframe sequence (Theatre.js or a baked animation clip), not from ad hoc per-frame math.</li><li>There MUST be one named shot per narrative section, indexed by that section's normalized progress from TASK-SCENE-007.</li><li>Camera updates MUST be driven by the scene progress map each frame, and MUST interpolate smoothly between adjacent shots.</li><li>When motion is not allowed the camera MUST hold a single authored default pose and MUST NOT animate.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Each section maps to a distinct, named camera shot.</li><li>Scrolling between sections moves the camera smoothly along the keyframes.</li><li>With reduced motion, the camera stays at the default pose.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Not yet implemented; acceptance pending build.</p>";
