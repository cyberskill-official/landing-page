window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BIZ-017"]="<h1>TASK-BIZ-017: Unblock the practice subdomain exam routes in robots.txt so the ccaf redirects can transfer</h1>\n<h2>0. Why (evidence)</h2>\n<p>This task is off-site for this repository: the fix lands in the practice/ccaf application, not in landing-page. It is filed here because the evidence arrived through this property's Search Console and because TASK-SEO-022 links toward the affected pages.</p>\n<blockquote><p><strong>Revised 2026-07-30 after inspecting the repo and re-measuring.</strong> The original §0 asserted that the ccaf host served its own blocking robots.txt and that this made its 301s uncrawlable. That was an artefact of a <code>curl -L</code> which followed the redirect and displayed practice's robots.txt. The ccaf host serves no robots.txt and 301s every path. The real defect is on the destination host and is larger than first described.</p></blockquote>\n<p>Measured 29-30 Jul 2026:</p>\n<ul><li><code>practice.cyberskill.world/robots.txt</code> contained <code>Disallow: /exam</code>. robots.txt matches by PREFIX, so that rule also matched the whole <code>/exams/*</code> tree: <code>/exams</code>, every <code>/exams/{code}</code>, every <code>/exams/{code}/sample-questions</code>, and every pSEO path.</li><li>Resolved against the live sitemap under RFC 9309 (longest match wins), <strong>25 of 52 sitemap URLs, 48 percent, were blocked</strong> - the site advertised URLs its own robots.txt forbade fetching.</li><li><code>ccaf.cyberskill.world</code> 301s every path including <code>/robots.txt</code>, so nothing on the legacy host is blocked and every redirect is crawlable. Already correct.</li><li>The Bing keyword report's entire demand cluster (<code>ccaf mock exam</code>, <code>claude certified architect practice test</code>, <code>anthropic mock exam</code>, <code>free claude ai mock exam</code>) targets exactly the pSEO pages inside the blocked tree.</li></ul>\n<p>The likely original intent was to keep a logged-in exam session out of the index. <code>Disallow</code> is the wrong instrument for that, on two counts. It blocks crawling, not indexing, so a blocked URL can still be indexed from external links with no snippet. And <code>src/lib/urlContract.ts</code> already declares those routes \"must carry noindex\" - which all six do - but <code>noindex</code> can only be obeyed if the crawler may fetch the page and read it. The Disallow list was simultaneously too broad and redundant with the mechanism it was suppressing.</p>\n<p>The copy-pasteable patch, with the diffs, the per-path judgement table and the order of operations, is in <code>docs/ops/practice-subdomain-seo-patch.md</code>.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>ccaf.cyberskill.world</code> SHALL serve no blocking robots.txt, so every 301 on the legacy host stays crawlable and the migration can consolidate. <strong>Verified 2026-07-30: already true.</strong> The middleware 301s every path including <code>/robots.txt</code>; the earlier claim to the contrary was a <code>curl -L</code> artefact. No change required.</li><li>1.2 <code>practice.cyberskill.world/robots.txt</code> MUST remove the <code>Disallow</code> rules for any path intended to rank, and SHALL retain <code>Disallow</code> only for genuinely non-public surfaces (<code>/api/</code>, <code>/admin</code>, <code>/account</code>, <code>/dashboard</code>). The operative defect was <code>Disallow: /exam</code>, which by prefix matching also blocked the whole <code>/exams/*</code> tree: 25 of 52 sitemap URLs.</li><li>1.3 Any route that must stay out of the index while remaining crawlable SHALL carry a <code>noindex</code> robots meta tag or <code>X-Robots-Tag</code> header instead of a robots.txt <code>Disallow</code>. <strong>Verified 2026-07-30: already true</strong> for all six runtime routes via <code>runtimeNoIndex</code>; the robots.txt block was what made those directives unreadable.</li><li>1.4 A <code>Sitemap:</code> directive SHALL reference a sitemap on its own host. <strong>Verified 2026-07-30: already true</strong> — practice's robots.txt names practice's own sitemap, and ccaf serves no robots.txt. No change required.</li><li>1.5 The ccaf host SHALL keep its 301 responses in place for at least twelve months after the block is lifted, so Google has a full recrawl window to observe them.</li><li>1.6 The practice application SHALL serve an IndexNow key file at the site ROOT and submit its own URL set, mirroring TASK-SEO-021. Root placement is required, not stylistic: the protocol scopes a submission to the directory holding the key file, so a key under a subdirectory cannot authorise the whole host.</li><li>1.7 A regression test MUST assert that no <code>Disallow</code> rule prefix-shadows any path in <code>indexedPaths()</code>, so the same class of defect cannot return under a different path name, and that test MUST be shown to fail against the pre-fix rule set.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - the ccaf host serves no blocking robots.txt and 301s every path, confirmed without following redirects - evidence: <code>docs/verification/task-biz-017-practice-robots-2026-07-30.md</code> §1</li><li>[ ] AC for 1.2 - zero of the sitemap's URLs are blocked under RFC 9309 longest-match resolution, and Disallow covers only non-public surfaces - evidence: <code>docs/verification/task-biz-017-practice-robots-2026-07-30.md</code> §2, §4</li><li>[ ] AC for 1.3 - every runtime route carries noindex and is now fetchable so the directive can be read - evidence: <code>docs/verification/task-biz-017-practice-robots-2026-07-30.md</code> §3</li><li>[ ] AC for 1.4 - each host's Sitemap directive names its own host - evidence: <code>docs/verification/task-biz-017-practice-robots-2026-07-30.md</code> §1</li><li>[ ] AC for 1.5 - the redirect retention window is recorded with a review date - evidence: OUTSTANDING, operator</li><li>[ ] AC for 1.6 - the practice IndexNow key file returns 200 at the site root and a submission is accepted - evidence: OUTSTANDING, needs <code>INDEXNOW_KEY</code> on that project</li><li>[ ] AC for 1.7 - the robots contract test fails against the pre-fix rule set and passes after - evidence: <code>docs/verification/task-biz-017-practice-robots-2026-07-30.md</code> §5</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Lifting the block exposes any genuinely private page that was relying on robots.txt for privacy. Robots.txt was never a privacy control; audit each unblocked path for an authentication check before shipping 1.1 and 1.2.</li><li>An exam page that renders per-user state must not be cached by a search engine. <code>noindex</code> plus the existing <code>Cache-Control: private, no-store</code> on that route is the correct pairing, and that header is already present.</li><li>Removing robots.txt from the ccaf host entirely means the host must still answer the 301 for every path, including <code>/robots.txt</code> itself.</li><li>Google may take several weeks to recrawl the unblocked URLs. Absence of movement in the first fortnight is not evidence the fix failed.</li><li>If the practice pages carry a canonical pointing back to ccaf, the consolidation runs backwards; verify canonical direction before lifting the block.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Anything inside landing-page. The internal links from the main domain are TASK-SEO-022; landing-page IndexNow is TASK-SEO-021.</li><li>Content or ranking work on the practice pages themselves.</li><li>The <code>www.cyberskill.world</code> hostname, which currently does not resolve at all. That is a DNS matter and needs its own task.</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>Authentication is enforced in the application, never by robots.txt. Nothing here may become the only thing keeping a private page private.</li><li>Per-user exam state is never cacheable by an intermediary or a search engine.</li><li>The ccaf 301s stay intact for the full retention window; removing them early strands every external link that still points at the old host.</li></ul>";
