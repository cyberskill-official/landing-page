window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-BIZ-017"]="<h1>TASK-BIZ-017: Unblock the practice subdomain exam routes in robots.txt so the ccaf redirects can transfer</h1>\n<h2>0. Why (evidence)</h2>\n<p>This task is off-site for this repository: the fix lands in the practice/ccaf application, not in landing-page. It is filed here because the evidence arrived through this property's Search Console and because TASK-SEO-022 links toward the affected pages.</p>\n<p>Measured 29 Jul 2026:</p>\n<ul><li><code>https://ccaf.cyberskill.world/robots.txt</code> and <code>https://practice.cyberskill.world/robots.txt</code> both contain <code>Disallow: /exam</code> and <code>Disallow: /practice</code>.</li><li><code>https://ccaf.cyberskill.world/exam</code> returns <code>301 -&gt; https://practice.cyberskill.world/exam</code>.</li><li>The GSC coverage export lists 2 pages under \"Blocked by robots.txt\" and 2 under \"Page with redirect\".</li><li>The Bing keyword report ranks <code>https://ccaf.cyberskill.world/exam</code> at average position 1.5 with a 37.5 percent CTR, and the surrounding query cluster (<code>ccaf mock exam</code>, <code>claude certified architect practice test</code>, <code>anthropic mock exam</code>) is the site's entire organic demand.</li></ul>\n<p>The compounding failure is the interaction, not either fact alone. A <code>Disallow</code> on <code>ccaf.cyberskill.world/exam</code> forbids Google from fetching that URL at all, which means it never observes the 301 sitting behind it. A redirect that cannot be crawled cannot consolidate signals: the migration from ccaf to practice is currently dropping its accumulated authority rather than transferring it, and will keep doing so for as long as the source URLs stay disallowed.</p>\n<p>The likely original intent was to keep a logged-in exam session out of the index. <code>Disallow</code> is the wrong instrument for that. It blocks crawling, not indexing, and a blocked URL can still be indexed from external links with no snippet, which is the worst of both outcomes. <code>noindex</code> is the instrument that removes a page from the index, and it requires the page to be crawlable to be seen at all.</p>\n<p>The copy-pasteable patch, with the diffs, the per-path judgement table and the order of operations, is in <code>docs/ops/practice-subdomain-seo-patch.md</code>.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>ccaf.cyberskill.world/robots.txt</code> MUST remove <code>Disallow: /exam</code> and <code>Disallow: /practice</code> so the 301 responses on those paths become crawlable and the migration can consolidate.</li><li>1.2 <code>practice.cyberskill.world/robots.txt</code> MUST remove the <code>Disallow</code> rules for any path intended to rank, and SHALL retain <code>Disallow</code> only for genuinely non-public surfaces (<code>/api/</code>, <code>/admin</code>, <code>/account</code>, <code>/dashboard</code>).</li><li>1.3 Any route that must stay out of the index while remaining crawlable SHALL carry a <code>noindex</code> robots meta tag or <code>X-Robots-Tag</code> header instead of a robots.txt <code>Disallow</code>.</li><li>1.4 The <code>Sitemap:</code> directive in <code>ccaf.cyberskill.world/robots.txt</code> SHALL reference a sitemap on its own host, or the ccaf host SHALL serve only a redirect and no robots.txt of its own, because a cross-host sitemap reference is ignored.</li><li>1.5 The ccaf host SHALL keep its 301 responses in place for at least twelve months after the block is lifted, so Google has a full recrawl window to observe them.</li><li>1.6 The practice application SHALL serve an IndexNow key file and submit its own URL set, mirroring TASK-SEO-021, since Bing raises the same recommendation for that property.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - a fetch of the ccaf robots.txt shows no Disallow for /exam or /practice - evidence: <code>docs/verification/&lt;date&gt;-ccaf-robots.md</code></li><li>[ ] AC for 1.2 - a fetch of the practice robots.txt shows Disallow only for non-public surfaces - evidence: <code>docs/verification/&lt;date&gt;-practice-robots.md</code></li><li>[ ] AC for 1.3 - each formerly disallowed private route returns a noindex directive in its markup or headers - evidence: <code>docs/verification/&lt;date&gt;-practice-noindex.md</code></li><li>[ ] AC for 1.4 - the ccaf robots.txt either names its own host in the Sitemap directive or is no longer served - evidence: <code>docs/verification/&lt;date&gt;-ccaf-robots.md</code></li><li>[ ] AC for 1.5 - the redirect retention window is recorded with a review date - evidence: <code>docs/verification/&lt;date&gt;-ccaf-redirect-retention.md</code></li><li>[ ] AC for 1.6 - the practice IndexNow key file returns 200 and a submission is accepted - evidence: <code>docs/verification/&lt;date&gt;-practice-indexnow.md</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Lifting the block exposes any genuinely private page that was relying on robots.txt for privacy. Robots.txt was never a privacy control; audit each unblocked path for an authentication check before shipping 1.1 and 1.2.</li><li>An exam page that renders per-user state must not be cached by a search engine. <code>noindex</code> plus the existing <code>Cache-Control: private, no-store</code> on that route is the correct pairing, and that header is already present.</li><li>Removing robots.txt from the ccaf host entirely means the host must still answer the 301 for every path, including <code>/robots.txt</code> itself.</li><li>Google may take several weeks to recrawl the unblocked URLs. Absence of movement in the first fortnight is not evidence the fix failed.</li><li>If the practice pages carry a canonical pointing back to ccaf, the consolidation runs backwards; verify canonical direction before lifting the block.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Anything inside landing-page. The internal links from the main domain are TASK-SEO-022; landing-page IndexNow is TASK-SEO-021.</li><li>Content or ranking work on the practice pages themselves.</li><li>The <code>www.cyberskill.world</code> hostname, which currently does not resolve at all. That is a DNS matter and needs its own task.</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>Authentication is enforced in the application, never by robots.txt. Nothing here may become the only thing keeping a private page private.</li><li>Per-user exam state is never cacheable by an intermediary or a search engine.</li><li>The ccaf 301s stay intact for the full retention window; removing them early strands every external link that still points at the old host.</li></ul>";
