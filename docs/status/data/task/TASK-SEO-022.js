window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SEO-022"]="<h1>TASK-SEO-022: Link the practice subdomain from the main site so it stops being an orphan</h1>\n<h2>0. Why (evidence)</h2>\n<p>The Bing keyword report for cyberskill.world, 29 Jul 2026, is almost entirely one intent cluster. Of the top 50 queries, roughly 45 are CCAF mock-exam terms: <code>ccaf mock exam</code> (18 impressions, position 4.1), <code>claude certified architect practice test</code> (15), <code>claude certified architect mock exam</code> (14), <code>anthropic mock exam</code> (10). Several are navigational queries for the property itself - <code>cyberskill ccaf</code>, <code>ccaf cyberskill world</code>, <code>cyberskill ccaf exam</code> - all converting at 25 to 60 percent CTR from position 1.5. One query is the literal URL <code>https://ccaf.cyberskill.world/exam</code>.</p>\n<p>That demand lands on <code>practice.cyberskill.world</code> (migrated from <code>ccaf.cyberskill.world</code>, which now 301s). A grep across <code>lib</code>, <code>app</code> and <code>components</code> for <code>ccaf</code> or <code>practice.cyberskill</code> returns zero matches: the main domain does not link to the subdomain even once. The subdomain is an orphan carrying the site's entire organic demand, and the authority the main domain has accrued does not reach it.</p>\n<p>This is the cheapest available fix for the 60 URLs sitting in \"Discovered / Crawled - currently not indexed\": discovery and internal PageRank both follow links, and there are currently none to follow.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The site SHALL link to <code>https://practice.cyberskill.world</code> from at least one persistent, crawlable, site-wide surface, using a server-rendered anchor with a real <code>href</code>.</li><li>1.2 The link text SHALL be descriptive of the destination and SHALL NOT be a bare URL or a generic label such as \"click here\", so it carries the query intent the destination ranks for.</li><li>1.3 Both locales SHALL ship the link in the same commit, with the label sourced from the content or dictionary layer and never hardcoded in a component.</li><li>1.4 The destination URL SHALL be declared once in <code>lib/content/site.ts</code> alongside the other company facts, so no component holds a second copy.</li><li>1.5 The anchor SHALL NOT carry <code>rel=\"nofollow\"</code> or <code>rel=\"sponsored\"</code>, because the target is a first-party property and the link is intended to pass authority.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - the rendered footer markup for every route contains an anchor whose href is the practice subdomain - test: <code>seo/practice-link-present</code></li><li>[ ] AC for 1.2 - the link label matches neither a bare URL nor the generic-label denylist already used by the descriptive-link-text gate - test: <code>seo/practice-link-descriptive</code></li><li>[ ] AC for 1.3 - the EN and VN dictionaries both define the label, and a missing key fails the build - test: <code>seo/practice-link-bilingual</code></li><li>[ ] AC for 1.4 - the URL resolves from the site config and appears exactly once as a literal in the source tree - test: <code>seo/practice-link-single-source</code></li><li>[ ] AC for 1.5 - the anchor carries no nofollow or sponsored rel token - test: <code>seo/practice-link-follows</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The subdomain being down must not break the footer render: this is a plain anchor, not a fetch, so there is nothing to fail.</li><li><code>target=\"_blank\"</code> requires <code>rel=\"noopener\"</code>. That rel token is permitted and does not conflict with 1.5.</li><li>The Vietnamese label must not be an English string transliterated; the destination serves a <code>/vi</code> variant and the label should read naturally.</li><li>A future rename of the subdomain must be a one-line change, which is what 1.4 buys.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Content about the CCAF exam on the main domain. Publishing a landing page targeting those queries is a separate content decision (a CMS task).</li><li>The robots.txt rules on the practice subdomain that currently block the pages this task links toward (TASK-BIZ-017). Linking to a disallowed path is still correct: the link is the discovery path once the block is lifted.</li><li>Any change to the ccaf to practice redirect chain, which lives in the other repository.</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>EN and VN ship together; a key present in one dictionary and absent in the other is a build failure, not a silent fallback.</li><li>Company facts and first-party URLs have exactly one source in the repo.</li><li>No client component is added to the home page for this; a link is server-rendered DOM.</li></ul>";
