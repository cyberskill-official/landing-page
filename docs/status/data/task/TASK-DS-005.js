window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DS-005"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The team MUST settle, in writing, whether the published design-system packages can be consumed, because the rest of the DS work forks on that answer.</p>\n<ol><li>The investigation MUST determine whether a published CyberSkill design package is installable from a reachable registry under the project's credentials. (Historical names <code>@cyberskill/tokens</code> / <code>@cyberskill/react</code> / <code>@cyberskill/style-packs</code> were superseded by the monolith <code>@cyberskill/design</code>.)</li><li>If it installs, the project SHOULD consume it and apply the CyberSkill theme on top; if it does not, the project MUST keep the hand-ported tokens (TASK-DS-001) as the source of truth.</li><li>The outcome MUST be recorded as a dated decision note resolving the Phase 0 open question either way.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A reproducible install attempt is documented with its result (success or the exact failure).</li><li>A decision note states the chosen path and updates the dependency on TASK-DS-001.</li></ul>\n<h2>§3 Evidence</h2>\n<p><strong>Resolved 2026-07-24.</strong> <code>@cyberskill/design@1.0.0</code> installs from the public npm registry. First-slice adoption: package <code>styles.css</code>, Lumi identity attrs on <code>&lt;html&gt;</code>, live package <code>Button</code> on the consent banner, token bridge in <code>globals.css</code>. Decision: <code>docs/decisions/2026-07-24-cyberskill-design-package.md</code>. Hand-ported storytelling tokens remain until a follow-up migration.</p>";
