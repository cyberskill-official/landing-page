window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SEO-010"]="<h1>TASK-SEO-010: Restore word spacing in the machine-readable text of kinetic headings</h1>\n<h2>0. Why (evidence)</h2>\n<p>components/motion/KineticText.tsx renders each word in its own aria-hidden &lt;span&gt; with no whitespace between spans, so the serialized DOM text of the big section headings reads \"Thearcofawish\" and \"TurnYourWillIntoReal\". Screen readers are fine (the heading carries aria-label), but crawlers, AI answer engines and text extractors that ignore ARIA see run-together strings on the site's most important headings. Audit C names this as a Phase 1, low-effort, high-priority fix.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The serialized text content of every heading rendered through KineticText SHALL contain the same word boundaries as the visible text.</li><li>1.2 The fix SHALL preserve the existing accessibility contract: the heading keeps its aria-label, the visual word spans stay aria-hidden, and the reduced-motion / scripting:none guards still force the words visible.</li><li>1.3 The rendered visual output SHALL be pixel-identical to today's (no added gaps, no reflow).</li><li>1.4 A test SHALL fetch the server-rendered HTML of <code>/en</code> and <code>/vi</code> and assert that each kinetic heading's extracted text equals the intended string, spaces included.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - extracted text of every kinetic heading contains spaces - test: <code>seo/kinetic-heading-text</code></li><li>[ ] AC for 1.2 - axe reports no new violations; aria-label unchanged - test: <code>check:a11y:routes</code></li><li>[ ] AC for 1.3 - visual regression on the hero and section headings is nil - test: <code>visual/kinetic-heading-snapshot</code></li><li>[ ] AC for 1.4 - the SSR HTML assertion runs on both locales - test: <code>seo/kinetic-heading-text</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Vietnamese headings with diacritics and longer words must not wrap differently after the fix.</li><li>Headings that contain punctuation or a line break.</li><li>An sr-only duplicate must not be read twice by a screen reader.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Changing the heading copy.</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.4: WCAG 2.2 AA floor and prefers-reduced-motion support.</li><li>AGENTS.md §4.1 HTML-first: every meaningful state stays server-rendered DOM and the canvas never owns LCP.</li><li>AGENTS.md §4.5 Vietnamese-first: every user-facing string ships EN and VN in the same commit.</li></ul>";
