window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SEO-023"]="<h1>TASK-SEO-023: Emit x-default in the sitemap so it agrees with the head hreflang set</h1>\n<h2>0. Why (evidence)</h2>\n<p><code>resolveMetadata</code> in <code>lib/content/metadata.ts</code> emits three hreflang alternates into the document head: <code>en</code>, <code>vi</code>, and <code>x-default</code> pointing at the English URL. <code>app/sitemap.ts</code> builds its <code>alternates.languages</code> map with only <code>en</code> and <code>vi</code>. The live sitemap confirms it: every <code>&lt;url&gt;</code> entry carries two <code>xhtml:link</code> elements, never three.</p>\n<p>The two signals therefore disagree about whether an x-default exists. Google reconciles conflicting hreflang sets by discarding the annotation it cannot verify, so the cheapest reading of this is that the x-default is being ignored for every route. The comparison case is instructive: <code>practice.cyberskill.world/sitemap.xml</code> already emits all three, including x-default, on every entry.</p>\n<p>The GSC coverage export shows one page under \"Alternate page with proper canonical tag\", which is the expected and correct state for an hreflang pair and is not a defect. This task is not about that row. It is about removing a genuine inconsistency between two hreflang declarations of the same route set, which is the kind of ambiguity that suppresses the whole annotation.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every URL entry in the sitemap SHALL declare the same hreflang key set the document head declares for that route, including <code>x-default</code>.</li><li>1.2 The <code>x-default</code> target SHALL be the English URL for the route, matching the head exactly.</li><li>1.3 The head and the sitemap SHALL derive their alternate maps from one shared builder, so the two cannot drift again.</li><li>1.4 The sitemap's entry point to that builder SHALL reject a route outside the registry, because the sitemap only ever iterates the registry and a miss there means drift.</li><li>1.5 A routable page deliberately absent from the sitemap registry SHALL still receive a complete alternate set in its head, because the strict entry point guards the sitemap and MUST NOT make a live page fail to render.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - every entry the sitemap generates carries en, vi and x-default keys - test: <code>seo/sitemap-hreflang-complete</code></li><li>[ ] AC for 1.2 - the x-default href equals the en href for the same route - test: <code>seo/sitemap-x-default-target</code></li><li>[ ] AC for 1.3 - head and sitemap alternates are byte-identical for every route in the registry - test: <code>seo/hreflang-head-sitemap-parity</code></li><li>[ ] AC for 1.4 - the helper returns a three-key map for every registry route and rejects an unknown route - test: <code>seo/hreflang-helper-total</code></li><li>[ ] AC for 1.5 - /lite, which is routable but not in the registry, still resolves a three-key head alternate set - test: <code>seo/hreflang-helper-total</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The root route builds its path as an empty string, not <code>/</code>; the helper must produce <code>/en</code> and not <code>/en/</code> for it.</li><li><code>/lite</code> is a real, routable page that is deliberately not in the sitemap registry. A strict helper wired into <code>resolveMetadata</code> turns that documented fallback into a build failure, which is why the strict check guards only the sitemap entry point and the builder underneath stays total.</li><li>Adding a third locale later must extend both surfaces from the single helper, with x-default still resolving to the default locale rather than the newest one.</li><li>A route present in the registry but not routable in the app would put a 404 in the sitemap; that pairing is checked by the existing sitemap-completeness gate and is not re-litigated here.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Adding or removing routes from the sitemap (TASK-SEO-012 owns completeness and lastmod).</li><li>Canonical tag behaviour, which is already correct.</li><li>The \"Alternate page with proper canonical tag\" row in the GSC export, which is the expected state for a working hreflang pair and needs no fix.</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>The route registry stays the single source of truth for the site's URL set.</li><li>The head hreflang set is already correct and must not be reduced to match the sitemap; the sitemap moves toward the head, not the reverse.</li></ul>";
