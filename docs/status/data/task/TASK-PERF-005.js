window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PERF-005"]="<h1>TASK-PERF-005: Font subset, display, and preload strategy including Vietnamese glyphs</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §J, and now a named cause of the worst metric on the site: audit B attributes part of the 0.431 mobile CLS to the display font swapping without a matched fallback. app/layout.tsx already self-hosts Space Grotesk with the Vietnamese subset via next/font (keyless, size-adjusted fallback) - this task centralises and completes the strategy so TASK-PERF-007 can rely on it.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Self-hosted fonts SHALL be subset to the Latin and Vietnamese ranges so every accented glyph the content uses renders without a fallback swap.</li><li>1.2 Each web font SHALL declare an explicit <code>font-display</code> policy, and the primary text font SHALL be preloaded so first paint does not wait on a late fetch.</li><li>1.3 The fallback SHALL be metric-matched (size-adjust) so the swap produces no measurable layout shift in the hero or any heading.</li><li>1.4 The strategy SHALL be centralised in one module consumed by the root layout, so the policy is declared once.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - every Vietnamese diacritic in the content renders in the intended face - test: <code>content/vi-diacritics</code></li><li>[ ] AC for 1.2 - the primary font is preloaded and declares its display policy - test: <code>perf/lcp-preload</code></li><li>[ ] AC for 1.3 - the font swap contributes zero layout shift - test: <code>perf/font-swap-no-shift</code></li><li>[ ] AC for 1.4 - no component imports a font directly; all font usage flows through the module - test: <code>lint/font-single-source</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A Vietnamese glyph missing from the subset falls back per-glyph and produces a visibly mixed typeface.</li><li>Preloading too many weights costs more than it saves.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>The rest of the CLS fix (TASK-PERF-007).</li></ul>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>AGENTS.md §4.7: the CI performance budget (lighthouse/budget.json) is never relaxed to make a gate green.</li><li>AGENTS.md §4.5 Vietnamese-first: every user-facing string ships EN and VN in the same commit.</li><li>AGENTS.md §4.2: every secret lives only in server env; no NEXT_PUBLIC_ secret, ever (NFR-SEC-001).</li></ul>";
