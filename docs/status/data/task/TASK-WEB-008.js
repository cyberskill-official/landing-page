window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-008"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Each service SHOULD have its own crawlable detail page per locale, so the offer can rank and be linked on its own URL.</p>\n<ol><li>The route <code>/[lang]/services/[slug]</code> MUST exist and MUST use <code>generateStaticParams</code> to pre-render every service in both locales from the content source (TASK-CMS-005).</li><li>Each page MUST emit per-locale metadata and hreflang alternates (<code>en</code>, <code>vi</code>, <code>x-default</code>), and an unknown slug MUST <code>notFound()</code>.</li><li>Page copy MUST come from the shared content source for the active locale, not be duplicated in the route.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Every service resolves at <code>/en/services/[slug]</code> and <code>/vi/services/[slug]</code> with correct hreflang links; an unknown slug returns 404.</li><li>View-source shows the service title and body as text per locale.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Not yet implemented; acceptance pending build.</p>";
