window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Because a canvas exposes no indexable text, the meaningful facts MUST live in the DOM and in structured data.</p>\n<ol><li>The app MUST emit a sitemap and a robots policy via <code>app/sitemap.ts</code> and <code>app/robots.ts</code>, listing every meaningful URL for both locales.</li><li>Each meaningful state MUST map to exactly one indexable URL; the home, <code>/work</code>, and <code>/careers</code> routes MUST publish <code>en</code> and <code>vi</code> hreflang alternates plus <code>x-default</code>.</li><li>The home page MUST embed Organization JSON-LD (name, HCMC address, contact) and FAQ JSON-LD whose answers match the on-page copy.</li><li>Each route MUST emit OpenGraph metadata and reference <code>public/favicon.svg</code>.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li><code>/sitemap.xml</code> and <code>/robots.txt</code> resolve and include both locales.</li><li>Home source contains valid <code>application/ld+json</code> for Organization and FAQ.</li><li>hreflang alternates are present and self-consistent across locales.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: sitemap/robots route handlers + both JSON-LD components + favicon authored; hreflang emitted from the locale metadata. Deferred: Rich Results and sitemap fetch validation on the operator machine after <code>next build</code>.</p>";
