window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CTA-013"]="<h1>FR-CTA-013: Mark required fields in the markup, validate, and verify the honeypot</h1>\n<h2>0. Why (evidence)</h2>\n<p>Audit C: the classic contact form marks no fields as required in the markup - not name, email or message - so a no-JS or assistive-technology user gets no native constraint and incomplete submissions are only caught client-side by react-hook-form. Audit C also asks to confirm the leading input named <code>website</code> is a honeypot properly hidden from real users and from assistive technology (LeadForm.tsx renders it with a visible &lt;label&gt;Leave this empty&lt;/label&gt; and tabIndex=-1).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every required field SHALL carry the native <code>required</code> attribute and <code>aria-required</code>, in addition to the existing schema validation.</li><li>1.2 Server-side validation SHALL remain authoritative: the API SHALL reject an incomplete or malformed payload with a specific error, regardless of what the client sent.</li><li>1.3 The honeypot field SHALL be hidden from sighted users and from assistive technology (<code>aria-hidden</code>, off-screen or hidden, <code>tabIndex=-1</code>, <code>autocomplete=\"off\"</code>) and SHALL NOT be announced by a screen reader.</li><li>1.4 A submission that fills the honeypot SHALL be discarded server-side and SHALL NOT reach any sink.</li><li>1.5 Error messages SHALL be associated with their field (<code>aria-describedby</code>) and announced, in EN and VN.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - required and aria-required present on name, email and message - test: <code>cta/form-required-attrs</code></li><li>[ ] AC for 1.2 - the API rejects a payload missing email with a 400 and a named error - test: <code>api/lead-validation</code></li><li>[ ] AC for 1.3 - axe reports the honeypot as not exposed; a screen-reader snapshot omits it - test: <code>check:a11y:routes</code></li><li>[ ] AC for 1.4 - a honeypot-filled payload reaches no sink - test: <code>api/lead-honeypot</code></li><li>[ ] AC for 1.5 - errors are announced and associated, both locales - test: <code>a11y/form-errors</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A no-JS submit must produce a usable native validation experience.</li><li>Consent checkbox: required, and its refusal must not silently submit.</li><li>A legitimate visitor whose autofill fills the honeypot - keep the field name unattractive to autofill.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Rate limiting (FR-CHAR-029).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>AGENTS.md §4.4: WCAG 2.2 AA floor and prefers-reduced-motion support.</li><li>AGENTS.md §4.5 Vietnamese-first: every user-facing string ships EN and VN in the same commit.</li><li>A lead is never accepted without the consent checkbox (PDPL).</li></ul>";
