window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-006"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Unhandled errors MUST be reported somewhere a human will see them.</p>\n<ol><li>An error-tracking service (Sentry or an equivalent) MUST capture unhandled exceptions and promise rejections from the client.</li><li>The same service MUST capture server-side exceptions from the API routes and server components, with enough context to locate the fault.</li><li>Reported events MUST carry release and environment tags so a regression can be tied to the deploy that introduced it.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A thrown client error appears in the tracking dashboard.</li><li>A thrown server error appears with a stack trace.</li><li>Events are tagged with release and environment.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22 with Sentry (<code>@sentry/nextjs</code> 10.x), the operator's chosen service. <code>instrumentation-client.ts</code> initializes the browser SDK, whose default integrations capture unhandled exceptions and promise rejections (clause 1). <code>instrumentation.ts</code> initializes the server/edge SDK in <code>register()</code> and exports <code>onRequestError = Sentry.captureRequestError</code>, so exceptions from route handlers, server components, and middleware are captured with context (clause 2). Both inits set <code>environment</code> (VERCEL_ENV / NODE_ENV) and <code>release</code> (VERCEL_GIT_COMMIT_SHA server-side; NEXT_PUBLIC_SENTRY_RELEASE client-side), so events are tied to the deploy (clause 3). Everything is gated on <code>NEXT_PUBLIC_SENTRY_DSN</code> (<code>enabled: Boolean(dsn)</code>), a clean no-op until set.</p>\n<p>Implementation note: wired via the runtime SDK + Next instrumentation hooks only, deliberately without <code>withSentryConfig</code>, to avoid the build-time webpack plugin and sourcemap-upload step destabilizing the production build (the earlier Speed Insights dependency conflict is the cautionary precedent). Events still report; stack traces are unminified-readable once you add a sourcemap step later if wanted. Verified by tsc + lint + 37 vitest tests + <code>next build</code> (rc=0, 26/26 pages, instrumentation compiles). Activation: set <code>NEXT_PUBLIC_SENTRY_DSN</code> in Vercel; live capture confirmed once the DSN is present.</p>";
