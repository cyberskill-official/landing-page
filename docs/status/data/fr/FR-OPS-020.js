window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-020"]="<h1>FR-OPS-020: Lead dual-write to CyberOS and historical migration</h1>\n<h2>0. Why (evidence)</h2>\n<p><code>/api/lead</code> already best-effort fans out to CyberOS when <code>LEAD_CRM_WEBHOOK_URL</code> is set and also persists via <code>lib/db</code> (InMemory/Prisma). Without an explicit dual-write + migration FR, historical local leads never enter CyberOS and operators may treat Prisma as a second CRM.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 On every accepted lead, the handler SHALL attempt CyberOS intake as the <strong>primary SoR write</strong>; local <code>DbAdapter.saveLead</code> remains best-effort durability and must not replace CyberOS when both are configured.</li><li>1.2 CyberOS failure SHALL NOT fail the visitor response when another ack path succeeded; FR-OPS-010 style alerts SHALL fire.</li><li>1.3 A documented one-time migration SHALL export historical local leads (and linkable transcripts where present) into CyberOS without duplicating already-synced ids.</li><li>1.4 Intent, source, locale, and UTM fields SHALL survive dual-write unchanged (FR-OPS-011 / FR-CTA-006 mapping).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - with both CyberOS and DB configured, both receive the lead - test: <code>lead/dual-write</code></li><li>[ ] AC for 1.2 - CyberOS 5xx still returns ok to the visitor when validation passed - test: <code>lead/cyberos-outage-failopen</code></li><li>[ ] AC for 1.3 - migration script or runbook processes a fixture set idempotently - evidence: docs + dry-run log</li><li>[ ] AC for 1.4 - CRM mapped payload equals submitted intent/source/locale/utm - test: <code>api/lead-intent-routing</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Partial transcript already in local DB before CyberOS existed.</li><li>Replayed migration must not create duplicate CyberOS contacts.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Building CyberOS UI for SLA (FR-BIZ-009).</li><li>Content read model (FR-OPS-019).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>A submitted lead is never silently lost.</li><li>Secrets only in server env.</li></ul>";
