window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-010"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>GLB loading MUST stay off the first-paint critical path.</p>\n<ol><li>GLB assets MUST load through a preloader and a React Suspense boundary so the scene mounts only after its assets resolve.</li><li>The static poster MUST remain the first paint and MUST stay visible until the scene is ready to swap in.</li><li>A load failure MUST resolve to the poster fallback and MUST NOT throw to the page or leave a blank canvas.</li><li>Preloading MUST be triggered only behind the CanvasMount gate so mobile and reduced-motion visitors never fetch the GLB.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The poster paints first; the scene swaps in once assets resolve.</li><li>A failed GLB load falls back to the poster with no thrown error.</li><li>On a gated-out device, no GLB request is made.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-24 on branch <code>auto/glb-perf-a11y</code>.</p>\n<ul><li><code>components/canvas/GenieScene.tsx</code> reads <code>NEXT_PUBLIC_LUMI_GLB</code> and, when set, calls <code>useGLTF.preload(url)</code> at module scope. GenieScene is imported only by <code>CanvasMount</code> via <code>dynamic(..., { ssr: false })</code>, which mounts only on capable desktops, so the preload (and the GLB fetch) never fires on mobile or reduced-motion clients (criterion 4).</li><li>The Lumi render sits inside <code>&lt;Suspense fallback={null}&gt;</code>; the server-rendered static poster is the first paint and stays until the scene chunk + GLB resolve (criteria 1-2).</li><li><code>components/canvas/GlbBoundary.tsx</code> is an in-canvas React error boundary wrapping <code>GltfLumi</code>. A rejected GLB load (missing file, decode failure) is caught and the procedural <code>LumiPlaceholder</code> renders instead, so the canvas is never blank and nothing throws to the page (criterion 3). The procedural Lumi is a strictly richer fallback than the static poster.</li><li>Build-verified both ways: default (<code>next build</code>) and with <code>NEXT_PUBLIC_LUMI_GLB=/models/lumi.glb next build</code> both compile and prerender 26/26 pages. Integration steps: <code>docs/3d/lumi-glb-integration.md</code>.</li></ul>";
