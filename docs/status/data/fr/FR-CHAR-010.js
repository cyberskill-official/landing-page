window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CHAR-010"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The browser MUST never see the Anthropic key; it talks only to our proxy.</p>\n<ol><li><code>ANTHROPIC_API_KEY</code> MUST be read server-side only. The client MUST call <code>/api/genie</code> and MUST NOT call the Anthropic API directly.</li><li>The handler MUST call the Messages API with <code>stream: true</code> and MUST re-stream the model output to the browser as plain text chunks.</li><li>The handler MUST apply a per-IP, in-memory rate limit (best-effort) and MUST validate the request body before calling upstream.</li><li>When no key is configured the handler MUST return HTTP 503 so the client can fall back to the contact form.</li><li>The model id and max tokens MUST come from environment variables (<code>GENIE_MODEL</code>, token budget), not hardcoded.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A request with the key set streams plain text back token-by-token.</li><li>With the key unset, the route returns 503 and the UI offers the form.</li><li>Rapid repeat requests from one IP are throttled.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>app/api/genie/route.ts</code> reads <code>process.env</code>, streams text, validates input, returns 503 when keyless, and rate-limits per IP. Deferred: live stream and throttle behaviour on the operator machine with a real key.</p>";
