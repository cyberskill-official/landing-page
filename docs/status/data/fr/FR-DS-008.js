window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-008"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Type MUST be tokenized and the chosen typeface MUST render Vietnamese correctly, since Vietnamese is a first-class locale for this site.</p>\n<ol><li>The system MUST express the type scale (sizes, line heights, weights) as <code>--cs-*</code> tokens that components consume rather than literal pixel values.</li><li>The primary typeface MUST cover the full Vietnamese diacritic set, including stacked tone marks, with no tofu or fallback substitution on any in-content Vietnamese string.</li><li>Font loading MUST avoid invisible text that blocks first paint and SHOULD use a swap-friendly strategy.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A Vietnamese pangram with stacked diacritics renders in the chosen face with no glyph falling back.</li><li>Headings and body read their size and line height from <code>--cs-*</code> tokens.</li></ul>\n<h2>§3 Evidence (2026-07-02)</h2>\n<ul><li>Display face is now Space Grotesk via <code>next/font/google</code> with explicit <code>subsets: [\"latin\", \"vietnamese\"]</code> (app/layout.tsx). next/font fails the build if a declared subset does not exist, so the Vietnamese coverage is build-verified; the woff2 files are self-hosted into <code>.next/static/media</code> at build time (keyless, no runtime request to Google), <code>display: \"swap\"</code> with next/font's size-adjusted fallback (no invisible text, no swap CLS).</li><li>The old stack (Iowan Old Style/Palatino/Georgia) lacked Vietnamese diacritics: VN headings rendered with per-glyph fallback substitution (mixed typefaces mid-word) - the operator-reported bug. Visual QA screenshots (puppeteer, /vi at 1440x900, dark + light) now show the full slogan \"Hiện Thực Hoá Ý Chí\", section headings, and StoryArc beats (stacked marks: ệ, ự, ẫ, ổ) in one face with no substitution.</li><li>Scale stays tokenized: sizes read <code>--cs-text-*</code>, the face reads <code>--cs-font-display</code> (<code>var(--font-display, ...)</code> so isolated renders and tests keep a sane fallback), and the new <code>--cs-font-mono</code> token feeds the HUD micro-meta. Display tuning for the grotesk: h1 700 / -0.03em / 1.02, h2-h3 600 / -0.02em, <code>--cs-text-4xl</code> up to 5.6rem.</li><li>Gate green on the Mac (2026-07-02): tsc, vitest 16 files / 60 tests, lint, next build 26/26 pages, check:assets, served-route jsdom axe 0 violations on /en + /vi. lib/theme/typography.ts was not needed - tokens live in globals.css like every other DS token.</li></ul>";
