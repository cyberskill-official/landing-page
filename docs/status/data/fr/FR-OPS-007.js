window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Speed Insights MUST be installable without breaking the test toolchain.</p>\n<ol><li>The Vercel Speed Insights component MUST be mounted in the root layout so field metrics are reported from every route.</li><li>The install MUST resolve the known <code>@vercel/*</code> and Vitest Vite peer conflict; a committed <code>.npmrc</code> with <code>legacy-peer-deps=true</code> MUST make the install deterministic.</li><li>The dependency note MUST be recorded so the next operator does not rediscover the conflict by hitting it.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The Speed Insights component is mounted in the layout.</li><li>A clean install succeeds with the committed <code>.npmrc</code>.</li><li>Vitest still installs and runs alongside <code>@vercel/*</code>.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped. <code>@vercel/speed-insights@^2</code> (and <code>@vercel/analytics@^2</code>) are in <code>package.json</code>, and <code>&lt;SpeedInsights /&gt;</code> (+ <code>&lt;Analytics /&gt;</code>) are mounted in <code>app/layout.tsx</code>, so field metrics report from every route. The peer conflict this FR anticipated no longer reproduces: a clean <code>npm install</code> succeeds with no <code>.npmrc</code> (the dependency tree resolved cleanly once the packages landed via the Vercel install PRs), and Vitest still installs and runs alongside them (37 tests pass). So no <code>.npmrc</code> was needed; the note for the next operator is simply that the conflict is gone. Verified: clean install + vitest + next build green.</p>";
