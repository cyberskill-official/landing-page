window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CTA-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The form MUST stay low-friction and MUST validate on the server, never trusting the client.</p>\n<ol><li>The form MUST show at most five visible fields (name, email, company, intent, message) plus a required consent checkbox and a hidden honeypot field.</li><li>The same Zod schema MUST validate input on the client (react-hook-form) and again in the route handler; the server MUST reject payloads that fail.</li><li>A tripped honeypot MUST return a fake success (HTTP 200) and MUST NOT fan out to any downstream, so bots get no signal.</li><li>Every valid submission MUST be logged server-side. The handler SHOULD fan out best-effort to a Slack webhook and a CRM webhook; a failed webhook MUST NOT fail the user's request.</li><li>No secret (webhook URL or token) may reach the client bundle.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Missing consent or a malformed email is rejected by the server even if the client check is bypassed.</li><li>A filled honeypot yields 200 with no Slack/CRM call.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: shared schema in <code>lib/lead/schema.ts</code> imported by both the form and the route; honeypot + consent fields present; webhook reads use <code>process.env</code>. Deferred: live POST + webhook delivery test on the operator machine.</p>";
