window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>There MUST be one canonical scroll loop, and it MUST yield to reduced motion.</p>\n<ol><li>Lenis MUST own smooth scrolling. GSAP's ticker MUST drive <code>lenis.raf</code> with Lenis <code>autoRaf: false</code> and GSAP <code>lagSmoothing(0)</code>, so there is a single RAF source.</li><li>ScrollTrigger MUST update on the Lenis scroll event, keeping scene and triggers in sync with the smoothed position.</li><li>The scroll modules MUST be dynamically imported and MUST fail closed: a load failure MUST leave native scrolling intact.</li><li>Under reduced motion the provider MUST no-op, retaining the native scrollbar and skipping Lenis and the GSAP loop entirely.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>One RAF loop drives scroll (no double-RAF); ScrollTrigger tracks Lenis.</li><li>With motion reduced, native scrolling is used and Lenis never initialises.</li><li>A forced import failure falls back to native scroll without errors.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>lib/scroll/lenis-gsap.ts</code> wires the ticker with <code>autoRaf:false</code> and <code>lagSmoothing(0)</code>; <code>ScrollStoryProvider</code> no-ops under reduced motion and imports dynamically. Deferred: scroll feel and sync on the operator machine.</p>";
