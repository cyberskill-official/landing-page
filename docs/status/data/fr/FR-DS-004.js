window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-004"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The Liquid Glass language MUST be a complete, tokenized material set so every surface picks a named weight rather than inventing its own blur.</p>\n<ol><li>The system MUST define the five materials (whisper, light, standard, heavy, solid) as <code>--cs-glass-*</code> custom properties with matching <code>.cs-surface-*</code> classes.</li><li>Each material MUST collapse to a solid, legible surface under <code>prefers-reduced-transparency</code>, <code>forced-colors</code>, <code>@supports not (backdrop-filter)</code>, and <code>@media print</code>.</li><li>The materials MUST stay within the doctrine palette and MUST NOT hardcode brand hex outside the token block.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>All five <code>.cs-surface-*</code> classes resolve and apply their <code>--cs-glass-*</code> values.</li><li>Disabling transparency or backdrop-filter, forced-colors, and print each yield a solid surface with readable content.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped. <code>app/globals.css</code> defines all five materials as <code>--cs-glass-*</code> tokens (blur whisper/light/standard/heavy + tints, themed for light and dark) with matching <code>.cs-surface-whisper/-light/-standard/-heavy</code> glass classes and a <code>.cs-surface-solid</code> opaque class (§1.1). Every glass surface collapses to a solid <code>var(--cs-color-surface)</code> under all four conditions (§1.2): <code>@supports not (backdrop-filter)</code>, <code>@media (prefers-reduced-transparency: reduce)</code>, <code>@media (forced-colors: active)</code> (system palette + borders), and <code>@media print</code> (solid, no blur/shadow, plain border). All values are tokens - no brand hex is hardcoded outside the <code>:root</code>/<code>[data-theme]</code> token blocks (§1.3). Verified: next build rc=0.</p>";
