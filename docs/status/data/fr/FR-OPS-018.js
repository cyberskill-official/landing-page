window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-018"]="<h1>FR-OPS-018: Secure prune cron API route against spoofing via edge routing and signature validation</h1>\n<h2>0. Why (evidence)</h2>\n<p>The lead data pruning cron endpoint (/api/cron/prune) is critical for PDPL compliance. To prevent malicious actors from triggering unauthorized prune requests or attempting denial of service (DoS) attacks on the database, the endpoint must be locked down beyond a simple query secret.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The <code>/api/cron/prune</code> API route SHALL validate security headers (like <code>CRON_SECRET</code> env variables and provider-specific signature headers) before executing.</li><li>1.2 The route SHALL reject requests from unauthorized IP ranges or direct public requests if supported by the edge network.</li><li>1.3 Execution metrics (records pruned, duration) SHALL be logged securely with masking of any potentially sensitive debug outputs.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - requests missing the secret or signature are rejected with 401 Unauthorized - test: <code>security/cron-token-validation</code></li><li>[ ] AC for 1.2 - requests with expired signatures or bad headers fail immediately - test: <code>security/cron-signature-failure</code></li><li>[ ] AC for 1.3 - execution is audited in server logs without exposing record details - test: <code>security/cron-audit-logging</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Vercel or hosting platform system cron service updating signature keys, causing legitimate pruning tasks to fail verification.</li><li>Time drift between the signer and the receiver server breaking timestamp validation.</li></ul>\n<h2>4. Out of scope</h2>\n<ul><li>Implementation of global IP blocking rules (handled at CDN firewall / WAF).</li></ul>\n<h2>5. Protected invariants</h2>\n<ul><li>Restricting access to the cron route must never throw server errors that block other visitor API paths.</li></ul>";
