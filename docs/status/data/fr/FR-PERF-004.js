window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PERF-004"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The 3D scene MUST load after the page is usable, never before.</p>\n<ol><li>The build MUST expose a bundle analysis (for example via the Next.js bundle analyzer) so chunk composition is inspectable.</li><li>The 3D scene and its libraries MUST be dynamically imported into a separate chunk that is NOT part of the initial critical-path bundle.</li><li><code>docs/perf/bundle-audit.md</code> MUST record the audit findings and the per-chunk sizes so future regressions are comparable.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The bundle analyzer produces a per-chunk report on demand.</li><li>The 3D code ships in its own chunk, absent from the first load.</li><li>The audit doc lists current chunk sizes.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-24 on branch <code>auto/glb-perf-a11y</code>.</p>\n<ul><li>On-demand analyzer (criterion 1): <code>next.config.ts</code> wraps the config in <code>@next/bundle-analyzer</code>, gated by <code>ANALYZE=true</code>; <code>npm run analyze</code> emits <code>.next/analyze/{client,nodejs,edge}.html</code> per-chunk treemaps. Off by default, so normal builds are unchanged. Verified: the three reports were generated.</li><li>3D in its own chunk, absent from first load (criterion 2): the analyzer's chunk data shows the two first-load shared chunks (<code>352-*</code> 116 KB gzip, <code>4bd1b696-*</code> 53 KB gzip) contain no <code>three</code>, <code>@react-three/*</code>, or <code>gsap</code>/<code>lenis</code>. The 3D stack is split into async chunks (about 304 KB gzip total) fetched only when <code>CanvasMount</code> dynamically imports <code>GenieScene</code> (<code>ssr:false</code>) on capable desktops. First Load JS shared by all routes stays 176 KB.</li><li>Audit doc with current chunk sizes (criterion 3): <code>docs/perf/bundle-audit.md</code> records how to run the analyzer, the first-load and 3D async chunk tables with parsed + gzip sizes, totals (about 2249 KB parsed / 666 KB gzip / 43 chunks), and how to compare future regressions against the <code>check:assets</code> guard.</li></ul>";
