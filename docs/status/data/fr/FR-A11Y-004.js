window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-004"]="<h1>FR-A11Y-004: Full keyboard operability, visible focus, and focus order</h1>\n<h2>0. Why (evidence)</h2>\n<p>Research doc §H (WCAG 2.1.1 keyboard, 2.4.7 focus visible). Substantially in place already, and this FR closes the rest.</p>\n<p>Already true (2026-06): the site is HTML-first with native controls, a global 3px <code>:focus-visible</code> outline, a skip link, and the 3D canvas marked decorative (<code>aria-hidden</code>, <code>pointer-events:none</code>) so it is never a tab stop. The chat is a correctly-modelled non-modal <code>role=\"dialog\"</code> (<code>aria-modal=\"false\"</code>): focus moves to the input on open, returns to the launcher on close, Escape closes it, and the launchers carry <code>aria-haspopup=\"dialog\"</code> + <code>aria-expanded</code>. Per the WAI-ARIA APG a non-modal dialog does not trap focus, so there is no trap.</p>\n<p>Open: a real Tab / Shift+Tab pass across every route with no pointer, confirming order and the contrast of each focus indicator - and the 18 sub-44px controls audit B found (FR-A11Y-011), which are the same header icons and rail dots this FR must be able to reach.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every interactive element, including the chat widget, the chapter rail and the persistent CTA, SHALL be reachable and operable with the keyboard alone.</li><li>1.2 Every focusable element SHALL show a visible focus indicator meeting the WCAG 2.2 non-text contrast floor against its own background, in both themes.</li><li>1.3 Focus order SHALL follow the logical reading order, and no surface SHALL trap focus: focus SHALL always be able to leave.</li><li>1.4 When the chat or any overlay opens, focus SHALL move into it, and on close SHALL return to the control that opened it.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - a scripted Tab sweep reaches and activates every control on every route, EN and VN, with no pointer - test: <code>a11y/keyboard-sweep</code></li><li>[ ] AC for 1.2 - the focus ring passes contrast against every surface it lands on, dark and light - test: <code>check:apca</code></li><li>[ ] AC for 1.3 - the sweep never revisits a node twice and always escapes an open surface - test: <code>a11y/keyboard-sweep</code></li><li>[ ] AC for 1.4 - opening then closing the chat moves focus in and restores it - test: <code>a11y/chat-focus-return</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The scene canvas must stay out of the tab order even when it mounts.</li><li>A control that appears only on scroll (the persistent CTA) must still be reachable in order.</li><li>Escape inside the chat while a field has a pending value must not lose the value silently.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>The manual screen-reader pass (FR-A11Y-008).</li><li>Enlarging the tap targets (FR-A11Y-011).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>AGENTS.md §4.4: WCAG 2.2 AA floor and prefers-reduced-motion support.</li><li>AGENTS.md §4.1 HTML-first: every meaningful state stays server-rendered DOM and the canvas never owns LCP.</li></ul>";
