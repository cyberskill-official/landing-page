window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Content COULD be structured so generative answer engines can extract and cite CyberSkill directly.</p>\n<ol><li>Key pages COULD carry a short, self-contained summary near the top that states the answer in plain text before any elaboration.</li><li>Service and process pages SHOULD include question-and-answer blocks and one-sentence definitions that an engine can lift verbatim.</li><li>Cite-worthy facts MUST live in the DOM as readable text, never only inside the canvas or images.</li><li>Answer-oriented blocks SHOULD align with the FAQ JSON-LD from FR-SEO-001 so structured and prose forms agree.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A service page exposes a leading plain-text summary in the DOM.</li><li>Question-and-answer blocks and definitions are present and readable without the canvas.</li><li>FAQ prose matches the FAQ JSON-LD answers.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22 on the home page (the key page; dedicated <code>/services/[slug]</code> pages are FR-WEB-008, still planned). The existing DOM already satisfies the prose clauses: the Services section opens with a plain-text lead summary (clause 1) and gives each service a one-sentence definition; the FAQ section is readable text that mirrors the FAQ JSON-LD from FR-SEO-001/CMS-002 (clauses 2, 4); all of it lives in the DOM, not the canvas (clause 3). The added citable layer is <code>components/seo/ServicesJsonLd.tsx</code>, an <code>OfferCatalog</code> of <code>Offer</code> -&gt; <code>Service</code> entries built from the same <code>lib/content/site.ts</code> service facts, each provided by the <code>#organization</code> node, localized en/vi, rendered on the home page next to the FAQ JSON-LD. So an answer engine can extract the services as structured data that agrees with the visible prose. Verified by <code>next build</code> (rc=0) plus tsc + lint + vitest green.</p>";
