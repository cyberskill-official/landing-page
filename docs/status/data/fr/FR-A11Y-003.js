window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-003"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Accessibility regressions SHOULD be caught automatically before merge.</p>\n<ol><li>CI SHOULD run axe-core against the home, <code>/work</code>, <code>/careers</code>, and a case-study route on every build.</li><li>The check MUST fail the build when it finds violations at the serious or critical level.</li><li>The run MUST cover both locales for at least the home route so localised markup is exercised.</li><li>Each failure MUST report the rule, the offending selector, and the route so the fix is actionable.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The CI job runs axe-core across the listed routes on a pull request.</li><li>Introducing a serious or critical violation fails the build.</li><li>Failure output names the rule, selector, and route.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Partial (status stays planned). An automated axe gate runs in CI: <code>tests/axe.test.ts</code> composes the page's SSR-safe sections (Hero, TrustBand, ValueProp, Services, Process, WorkPreview, SocialProof, Careers, Faq, SiteFooter) into one <code>&lt;main&gt;</code>, renders them to server markup in jsdom, and runs <code>axe-core</code> over the combined DOM for both <code>en</code> and <code>vi</code>, failing on serious or critical violations (reported as rule + impact + node count). Composing the sections also exercises cross-section rules (duplicate ids, heading order, list/label structure), not just one component. It runs inside the existing vitest job the CI workflow already executes (<code>axe-core</code> + <code>jsdom</code> devDeps). Both locales currently pass with zero serious/critical violations. ContactCta is excluded because next/image does not render outside the Next runtime.</p>\n<p>Served-route gate added (2026-06-24, branch <code>auto/glb-perf-a11y</code>). <code>scripts/axe-routes.mjs</code> serves the production build (<code>next start</code>) and drives puppeteer (its own bundled Chrome) to run axe-core against the real rendered routes - <code>/en</code>, <code>/vi</code>, <code>/en/work</code>, <code>/en/careers</code>, and the <code>/en/work/operations-platform</code> case study - over the WCAG 2.x A/AA rule set, failing on serious/critical and printing rule + help URL + selector + route (criteria 1-4). It runs as a new <code>a11y</code> CI job (<code>npm run check:a11y:routes</code>).</p>\n<p>Verification done locally without a browser: the dev sandbox is arm64 and puppeteer ships an x86_64 Chrome that will not exec there, so the five routes were proven by fetching their real served-build SSR HTML and running axe-core in jsdom over each full page (one fresh process per route). All five pass the structural WCAG A/AA rules (landmark-one-main, region, bypass, document-title, html-has-lang, heading-order, link/button names, list structure, duplicate id) - 20-25 rules passed per route, zero serious/critical. <code>color-contrast</code> needs layout/paint that jsdom cannot do; it is covered by the CI Chrome job plus the APCA tooling (<code>npm run check:apca</code>).</p>\n<p>The CI job was introduced advisory (<code>continue-on-error: true</code>), mirroring how FR-PERF-002 rolled out, because puppeteer's served run could not be executed on the arm64 dev sandbox. It was then observed green on x86_64 CI - run 28089290130, the <code>Served-route axe gate</code> step passed (exit 0, all five routes clean with real Chrome including color-contrast) - so <code>continue-on-error</code> was removed and it is now a required, build-failing gate (criterion 2 met). All four acceptance criteria are satisfied. The component-level jsdom check in <code>tests/axe.test.ts</code> remains the always-on in-process guard alongside it.</p>";
