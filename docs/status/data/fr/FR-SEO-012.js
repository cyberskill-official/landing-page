window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-012"]="<h1>FR-SEO-012: Complete the sitemap and stamp real lastModified dates</h1>\n<h2>0. Why (evidence)</h2>\n<p>Audit B's crawl of the deployed sitemap found only the two homepages. The repository's app/sitemap.ts does emit services, work, careers and how-we-build - so either the deploy lagged or the crawl hit a stale cache; either way the omission of /privacy and /accessibility is real, and every entry stamps <code>new Date()</code> at build time, which is fake freshness that teaches crawlers to ignore lastmod.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 app/sitemap.ts SHALL enumerate every indexable route in both locales, including /privacy, /accessibility and (when it ships) /team and /notes, and SHALL exclude the noindex /lite route.</li><li>1.2 Each entry's lastModified SHALL come from a per-entry <code>lastUpdated</code> field in the content module, never from build time; the fallback SHALL be a fixed site-launch date.</li><li>1.3 A CI check SHALL fetch the built sitemap and assert the route count matches the indexable route set.</li><li>1.4 The deployed sitemap SHALL be re-verified against the live site after release and the result recorded.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - the built sitemap contains every indexable route x 2 locales and no /lite - test: <code>seo/sitemap-completeness</code></li><li>[ ] AC for 1.2 - two consecutive builds with no content change emit identical lastmod values - test: <code>seo/sitemap-stable-lastmod</code></li><li>[ ] AC for 1.3 - adding a route without a lastUpdated fails the check - test: <code>seo/sitemap-completeness</code></li><li>[ ] AC for 1.4 - the live sitemap is verified post-deploy and logged - test: <code>docs/post-deploy-verification</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A new content entry with no lastUpdated must fail loudly, not silently fall back.</li><li>hreflang alternates inside the sitemap must stay reciprocal.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Search Console submission (FR-BIZ-008).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>Nothing published may claim a fact, metric, credential or client the company cannot evidence.</li><li>AGENTS.md §4.5 Vietnamese-first: every user-facing string ships EN and VN in the same commit.</li></ul>";
