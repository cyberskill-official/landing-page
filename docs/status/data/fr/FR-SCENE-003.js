window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-003"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>On capable desktops the story MUST move; the device-capability gate MUST still protect weak devices. This overrides the reduced-motion gate from FR-A11Y-001 and FR-SCENE-002 per the decision record above.</p>\n<ol><li>The reduced-motion gate MUST be removed so the 3D scene and Lenis smooth scroll always run on capable desktops; the per-<code>&lt;html&gt;</code> motion toggle is retired.</li><li>Hero-scoped scroll progress MUST be published (0 at the top, 1 after one viewport) via a module-level store that the render loop reads directly, without triggering React re-renders.</li><li>The orb MUST rotate, drift toward the camera, and brighten as progress rises.</li><li>The device-capability gate MUST stay: mobile and low-end devices still get the static poster instead of the live scene.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>On a capable desktop, the scene and smooth scroll run with no motion toggle.</li><li>Scrolling the hero animates the orb (rotate, approach, brighten) tied to progress, and the progress store updates without React re-renders.</li><li>A mobile or low-end profile renders the static poster, not the canvas.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>lib/scroll/progress.ts</code> is a module-level store read in the render loop; <code>CanvasMount</code>/<code>ScrollStoryProvider</code>/<code>ScrollState</code> drop the reduced-motion gate and keep the capability gate; <code>LumiPlaceholder</code>/<code>GenieScene</code> map progress to rotation, drift, brightness, plus drei <code>Float</code> drift and golden <code>Sparkles</code>; <code>components/a11y/MotionToggle.tsx</code> removed. Deferred: scroll feel and frame rate on the operator machine.</p>";
