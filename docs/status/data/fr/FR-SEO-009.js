window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-009"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Page metadata COULD be derived from content through shared templates so titles and descriptions stay consistent and never go missing.</p>\n<ol><li>A shared helper COULD build each route's title, description, and OpenGraph fields from its content rather than hand-written tags per page.</li><li>Titles MUST follow one template per page type and MUST stay within sensible length limits for search results.</li><li>When a page omits a description, the helper MUST fall back to a content- derived summary, never an empty value.</li><li>The helper SHOULD localise output so <code>en</code> and <code>vi</code> pages read naturally in their own language.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Two pages of the same type produce titles in the same template shape.</li><li>A page with no explicit description still emits a non-empty, content-derived one.</li><li><code>vi</code> metadata reads in Vietnamese.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>lib/seo/metadata.ts</code> <code>pageMetadata()</code> builds title, optional description, alternates, OpenGraph, and Twitter for a route from caller-supplied content, and is applied to work, work/[slug], careers, privacy, and accessibility. The root layout's <code>title.template</code> (<code>\"%s - CyberSkill\"</code>) gives every page-type title one consistent shape (clause 2), and the layout's non-empty default <code>description</code> is the fallback when a page passes none, so a rendered description is never empty (clause 3). Descriptions are authored per locale, so <code>vi</code> pages read in Vietnamese (clause 4). <code>tests/seo-metadata.test.ts</code> asserts the title mirrors into OG/Twitter and the OG locale maps correctly. Verified by <code>next build</code> + tsc + lint green.</p>";
