window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-009"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Motion MUST be tokenized so every transition and the scene share one set of easing and duration values rather than each picking its own.</p>\n<ol><li>The system MUST define easing and duration motion tokens as <code>--cs-*</code> custom properties.</li><li>All UI transitions and the 3D scene MUST read timing from those tokens and MUST NOT hardcode ad hoc durations or cubic-beziers in components.</li><li>Under <code>prefers-reduced-motion: reduce</code>, token-driven transitions MUST reduce to no motion without breaking layout or state.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Easing and duration tokens are defined once and referenced by transitions and the scene.</li><li>With motion reduced, animated elements settle to their final state with no movement.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. The motion scale lives once in <code>:root</code> in <code>globals.css</code>: <code>--cs-ease</code> plus <code>--cs-dur-fast</code>/<code>--cs-dur</code>/<code>--cs-dur-slow</code>, with <code>--cs-dur-scroll</code> for the scene and <code>--cs-dur-shimmer</code> for the skeleton (clause 1). Every CSS transition references these tokens (header, cards, links, reveal, skeleton); the last hardcoded value (the skeleton's <code>1.4s ease</code>) now uses <code>var(--cs-dur-shimmer) linear</code>, so no component hardcodes an ad hoc duration or cubic-bezier (clause 2, CSS side). The 3D scene reads the same scale: <code>lib/motion/ tokens.ts</code> <code>readDurationSeconds()</code> parses <code>--cs-dur-scroll</code> from <code>:root</code> and <code>lib/scroll/lenis-gsap.ts</code> passes it to Lenis as the smooth-scroll <code>duration</code> (clause 2, scene side); <code>parseDurationToSeconds</code> is unit-tested in <code>tests/motion-tokens.test.ts</code>. Under <code>prefers-reduced-motion: reduce</code> the UI duration tokens collapse to <code>0.01ms</code> so token-driven transitions settle with no movement (clause 3), while <code>--cs-dur-scroll</code> is intentionally left intact because the storytelling motion is an explicit always-on product decision (see [[2026-06-22-always-motion-override]]). Verified by <code>next build</code> (rc=0) plus tsc + lint + vitest (32 tests) green.</p>\n<p>Note: per-frame lerp damping in <code>LumiPlaceholder</code> is frame-rate smoothing, not a design \"duration\", so it is deliberately not tokenized.</p>";
