window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The site MUST clear a WCAG 2.2 AA floor and MUST give users control over motion.</p>\n<ol><li>The app MUST honour <code>prefers-reduced-motion: reduce</code> and MUST also offer a manual motion toggle whose choice is persisted in <code>localStorage</code> and applied before animations start.</li><li>A <code>/lite</code> route per locale MUST mirror the full story as static DOM text, serving as the WCAG 2.3.3 alternative to the animated experience.</li><li>A skip link MUST be the first focusable element and MUST move focus to the main content target.</li><li>Reduced motion MUST disable scene mounting and reveal transforms, not merely shorten them.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Toggling motion off persists across reload and stops animation immediately.</li><li><code>/en/lite</code> and <code>/vi/lite</code> present the whole narrative as readable text.</li><li>Tab from page load lands on the skip link first.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>/lite</code> route + <code>SkipLink</code> + <code>MotionToggle</code> authored; toggle reads/writes <code>localStorage</code>; reduced-motion branch gates motion. Deferred: axe scan and VoiceOver/NVDA pass on the operator machine.</p>";
