window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-010"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Icons SHOULD be an in-repo SVG set with shared sizing so the visual weight is consistent and no outside icon dependency is pulled in.</p>\n<ol><li>The system SHOULD ship icons as in-repo SVGs rendered through one <code>Icon</code> component, with no external icon-library dependency.</li><li>Icon sizes MUST come from <code>--cs-*</code> sizing tokens, and color MUST inherit <code>currentColor</code> so icons follow the surrounding text token.</li><li>Decorative icons MUST be hidden from assistive tech, and meaningful icons MUST carry an accessible label.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Every icon renders through <code>Icon</code> at a token-driven size and inherits text color.</li><li>A decorative icon is <code>aria-hidden</code>; a meaningful one exposes a label.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>lib/icons/index.ts</code> holds the icon set as data (viewBox + primitive elements) with no external icon-library dependency; <code>components/ui/ Icon.tsx</code> is the single renderer (clause 1). Size comes from <code>--cs-icon-sm/md/lg</code> tokens via inline <code>width</code>/<code>height</code>, and <code>stroke=\"currentColor\"</code> makes icons follow the surrounding text token (clause 2). Icons are decorative by default (<code>aria-hidden</code>, <code>focusable=false</code>); passing <code>label</code> switches to <code>role=\"img\"</code> + <code>aria-label</code> (clause 3). The ad hoc inline SVGs in <code>ThemeToggle</code> (sun/moon) and <code>GenieChatPanel</code> (close) now render through <code>Icon</code>. As the accent sidecar Stephen asked for, <code>globals.css</code> adds an animated gradient <code>.cs-accent-divider</code> and a <code>.cs-sparkle</code> pulse (both honour reduced motion), and the hero eyebrow now carries a subtle decorative sparkle. <code>tests/icons.test.ts</code> asserts every icon has a valid viewBox and elements. Verified by tsc + lint + 37 vitest tests + <code>next build</code> (rc=0).</p>";
