window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CHAR-012"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The chat MUST cost nothing at first paint and MUST be operable by keyboard.</p>\n<ol><li>The chat panel MUST be lazily loaded with <code>ssr: false</code> so it never adds to the initial render or first paint.</li><li>The widget MUST read the <code>/api/genie</code> response stream token-by-token and append text as it arrives.</li><li>State MUST run through a Zustand store as an <code>idle -&gt; listening -&gt; thinking -&gt; speaking</code> machine.</li><li>The panel MUST be an ARIA dialog: Escape MUST close it and focus MUST be managed on open and close.</li><li>A consent line MUST be shown. The opening greeting MUST be UI-only so the API conversation history starts with the user's first turn.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The panel chunk loads only when opened; first paint excludes it.</li><li>Replies stream in; the state machine reflects listening/thinking/speaking.</li><li>Escape closes the dialog and returns focus to the opener.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: dynamic <code>ssr:false</code> panel, streaming reader, Zustand state machine, ARIA dialog + Escape + focus handling, consent line, UI-only greeting all authored. Deferred: live streaming and focus-trap testing on the operator machine.</p>";
