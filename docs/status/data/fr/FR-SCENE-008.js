window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-008"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The scene SHOULD prefer WebGPU but MUST never depend on it.</p>\n<ol><li>The renderer MUST attempt <code>WebGPURenderer</code> first when the browser reports WebGPU support.</li><li>On any WebGPU init failure or absence, the renderer MUST fall back to the WebGL renderer automatically, with no user-visible error.</li><li>The chosen backend MUST NOT change scene authoring: the same scene graph and materials MUST render on either path.</li><li>Backend selection MUST happen behind the existing CanvasMount gate so the poster fallback still owns the failure-closed path.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>On a WebGPU browser, the scene renders through WebGPURenderer.</li><li>On a WebGL-only browser, it renders through WebGL with no error.</li><li>A forced WebGPU init failure silently falls back to WebGL.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Not yet implemented; acceptance pending build.</p>";
