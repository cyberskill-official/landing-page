window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-004"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Long-form pages SHOULD expose article-level structured data so they can earn rich results and feed answer engines.</p>\n<ol><li>Each case study SHOULD embed <code>CreativeWork</code> JSON-LD, and each insight SHOULD embed <code>Article</code> JSON-LD, with headline, description, author, and publisher.</li><li>The structured data MUST carry <code>datePublished</code> and, when known, <code>dateModified</code>, both as ISO 8601 dates.</li><li>Headline and description MUST match the on-page title and summary so the markup never contradicts visible copy.</li><li>The publisher block MUST name CyberSkill and reference the same logo used by the Organization JSON-LD from FR-SEO-001.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A case-study source contains valid <code>CreativeWork</code> JSON-LD with author and publisher.</li><li>An insight source contains valid <code>Article</code> JSON-LD with <code>datePublished</code>.</li><li>Headline and description in the markup equal the rendered title and summary.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22 for the case-study scope. Each <code>work/[slug]</code> page emits inline <code>CreativeWork</code> JSON-LD whose <code>headline</code>/<code>name</code> equal the on-page h1 (<code>item.title</code>) and whose <code>description</code> equals the on-page lead (<code>item.result</code>), so markup and visible copy agree (clause 3). It carries <code>datePublished</code> and <code>dateModified</code> as ISO 8601 dates (clause 2), an <code>author</code> Organization, and a <code>publisher</code> that references the same <code>#organization</code> node emitted by <code>OrganizationJsonLd</code> in the <code>[lang]</code> layout (clause 4), so author and publisher resolve to one entity. Verified by <code>next build</code> (6 case-study pages prerendered) and tsc + lint green.</p>\n<p>Insight <code>Article</code> JSON-LD (clause 1, second half) is deferred: there is no insights route yet (FR-WEB-003 and the insights content FRs are still planned). When the insights route ships, the same pattern applies with <code>@type: Article</code>.</p>";
