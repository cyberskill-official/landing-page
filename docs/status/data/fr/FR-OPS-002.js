window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Funnel events MUST be measurable without cookies or third-party scripts, and the client tracker MUST never break the page.</p>\n<ol><li><code>/api/analytics</code> MUST validate an event-name allowlist and log accepted events server-side, optionally forwarding to a configured webhook.</li><li>A client helper MUST send events via <code>navigator.sendBeacon</code> with a keepalive-<code>fetch</code> fallback, and MUST never throw.</li><li>The <code>genie_open</code> and <code>lead_submitted</code> events MUST be wired. No cookies and no third-party scripts may be used.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A disallowed event name is rejected by the route; an allowed one is logged (and forwarded if a webhook is set).</li><li>Opening the Genie and submitting a lead each emit their event; a failed send is swallowed and the UI keeps working.</li><li>No cookie is set and no third-party analytics script is loaded.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>app/api/analytics/route.ts</code> checks the allowlist and logs/forwards server-side; <code>lib/analytics.ts</code> uses <code>sendBeacon</code> with a keepalive-<code>fetch</code> fallback wrapped so it cannot throw; <code>GenieOpenButton.tsx</code> and <code>LeadForm.tsx</code> fire <code>genie_open</code> / <code>lead_submitted</code>; <code>.env.example</code> documents the webhook. Deferred: live event capture on the operator machine.</p>";
