window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-004"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The bare <code>/</code> entry SHOULD send a first-time reader to the locale that fits their browser, while keeping the explicit switcher choice authoritative.</p>\n<ol><li>Middleware SHOULD parse the <code>Accept-Language</code> header on requests to <code>/</code> and redirect to <code>/en</code> or <code>/vi</code> by best match, defaulting sensibly when neither is preferred.</li><li>An explicit locale choice from the switcher MUST override negotiation on later visits, so a returning reader is not re-routed against their selection.</li><li>Negotiation MUST only choose among supported locales <code>{en, vi}</code> and MUST never produce an unsupported <code>lang</code>.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A browser preferring Vietnamese hitting <code>/</code> lands on <code>/vi</code>; one preferring English lands on <code>/en</code>.</li><li>After choosing a locale in the switcher, <code>/</code> honors that choice over the header.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>lib/i18n/negotiate.ts</code> <code>negotiateLocale()</code> parses the weighted <code>Accept-Language</code> list, sorts by q, and returns the first supported primary subtag (<code>en</code>/<code>vi</code>), falling back to the default for missing, wildcard, or unsupported-only headers; it can only ever return a supported locale (clause 3). The <code>/</code> handler in <code>middleware.ts</code> now prefers an explicit <code>cs-locale</code> cookie and falls back to negotiation, and <code>LanguageSwitcher</code> writes that cookie (one year, lax) on selection, so a returning reader's switcher choice overrides the header (clauses 1-2). <code>tests/i18n-negotiate.test.ts</code> covers vi-preferred, en-region, q-ordering, unsupported-skip, wildcard, and empty-header cases. Verified by <code>next build</code> (rc=0) plus tsc + lint + vitest green.</p>";
