window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Every case study MUST be discoverable from the sitemap.</p>\n<ol><li><code>app/sitemap.ts</code> MUST include every <code>/[lang]/work/[slug]</code> URL, each with <code>en</code> and <code>vi</code> alternates, so case studies are indexable.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The generated sitemap lists each work slug for both locales.</li><li>Each case-study entry carries <code>en</code> and <code>vi</code> alternates.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>app/sitemap.ts</code> maps over the work slugs and locales to emit each <code>/[lang]/work/[slug]</code> URL with <code>en</code>/<code>vi</code> alternates. Deferred: sitemap fetch and validation on the operator machine.</p>";
