window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-003"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Each case study MUST be a real, indexable page reachable from its card.</p>\n<ol><li>Dynamic <code>/[lang]/work/[slug]</code> pages MUST exist with <code>generateStaticParams</code> over locales x work slugs (6 prerendered) and MUST <code>notFound()</code> for an unknown slug.</li><li>Each page MUST emit per-locale metadata with a canonical URL and hreflang alternates, and MUST present bilingual challenge, what-we-did, and outcome prose.</li><li>Work cards MUST link to the detail page via an accessible stretched link.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li><code>/en/work/&lt;slug&gt;</code> and <code>/vi/work/&lt;slug&gt;</code> resolve for known slugs; 6 routes prerender; an unknown slug returns 404.</li><li>Each page exposes canonical + <code>en</code>/<code>vi</code> hreflang and reads in both locales.</li><li>A work card is fully clickable to its case study and remains keyboard reachable.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>app/[lang]/work/[slug]/page.tsx</code> defines <code>generateStaticParams</code> (locales x slugs), <code>notFound()</code> on miss, and per-locale canonical + hreflang metadata; <code>components/sections/WorkPreview.tsx</code> and <code>app/[lang]/work/page.tsx</code> add the stretched-link cards; styling in <code>app/globals.css</code>. Deferred: crawl and route smoke test on the operator machine.</p>";
