window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CMS-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The page MUST explain how we work and MUST answer common questions, and the visible FAQ MUST agree with the structured data.</p>\n<ol><li>A four-step how-we-work Process section MUST be added to the home page.</li><li>A visible FAQ section MUST be added, and its questions MUST match <code>components/seo/HomeFaqJsonLd.tsx</code> so the page and the FAQ JSON-LD agree.</li><li>Both sections MUST render in EN and VN.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The home page shows a four-step Process and a visible FAQ.</li><li>Every visible FAQ question matches a question emitted by <code>HomeFaqJsonLd.tsx</code>.</li><li>Both sections read correctly in <code>/en</code> and <code>/vi</code>.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>components/sections/Process.tsx</code> and <code>components/sections/Faq.tsx</code> are mounted in <code>app/[lang]/page.tsx</code>; the FAQ source array is shared with <code>components/seo/HomeFaqJsonLd.tsx</code> so page and structured data stay in sync; styling in <code>app/globals.css</code>. Deferred: rich-result validation on the operator machine.</p>";
