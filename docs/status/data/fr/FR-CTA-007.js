window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CTA-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The team MUST learn about a new lead in near real time so follow-up can start fast.</p>\n<ol><li>On each valid submission the handler MUST fan out a notification to a Slack webhook and SHOULD also send an email alert to the sales inbox.</li><li>The notification MUST carry enough to act on (name, company, intent, and a short message excerpt) and MUST NOT include the honeypot or raw secrets.</li><li>A failed notification MUST be logged and MUST NOT fail the user's submission.</li><li>The notification template MUST be bilingual-safe: it MUST render Vietnamese diacritics correctly and label fields in English for the internal team.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A valid lead posts a readable Slack message within seconds.</li><li>A notification outage still returns 200 to the user.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>app/api/lead/route.ts</code> fans each valid submission out to three best-effort channels via <code>Promise.allSettled</code>: an email to the company inbox (<code>company.email</code> = info@cyberskill.world) through Resend (raw fetch, gated on <code>RESEND_API_KEY</code>, <code>reply_to</code> set to the lead so a reply goes straight back), the existing Slack webhook (<code>LEAD_SLACK_WEBHOOK_URL</code>), and the CRM webhook (<code>LEAD_CRM_WEBHOOK_URL</code>) (clause 1). Each notification carries name, email, company, intent, locale/source, and the full message; the honeypot submission is dropped before any notification and no secret is included (clause 2). Failures are logged per channel and the handler always returns 200, so a notification outage never breaks the user's submit (clause 3). The body is UTF-8 text with English field labels, so Vietnamese diacritics render and the internal team reads consistent labels (clause 4).</p>\n<p>Activation note: set <code>RESEND_API_KEY</code> (and optionally <code>LEAD_EMAIL_FROM</code>, e.g. \"CyberSkill &lt;leads@cyberskill.world&gt;\" on a Resend-verified domain) in Vercel to turn the email on; until then it no-ops and the lead is still logged. Verified by tsc + lint + <code>next build</code>; the live send is confirmed once the key is set.</p>";
