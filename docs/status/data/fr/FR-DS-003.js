window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-003"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The repo MUST own its interactive primitives so the brand and accessibility contract is enforced in one place, not delegated to an outside library.</p>\n<ol><li>The system MUST ship Button, Field, Select, Dialog, and Card as in-repo components that reference only <code>--cs-*</code> tokens, and MUST NOT add an external component-library dependency.</li><li>Text on every primitive MUST meet APCA Lc &gt;= 75 for body and Lc &gt;= 90 for interactive labels against its rendered surface, including over Liquid Glass.</li><li>Each primitive MUST be keyboard operable and MUST expose its state (focus, disabled, invalid) through native semantics or ARIA, and the Dialog MUST trap focus and restore it on close.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Button, Field, Select, Dialog, and Card render from <code>--cs-*</code> tokens with no raw brand hex and no third-party component import.</li><li>Tab-only operation reaches and actuates every primitive; the Dialog returns focus to its trigger on close.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped. <code>components/ui/</code> now has Button, Card, Field, Select, and Dialog. Each emits the existing token-styled markup (<code>.cs-btn*</code>, <code>.cs-glass-card</code> / <code>.cs-surface-*</code>, <code>.cs-field</code>) - no raw brand hex, no third-party component dependency (§1.1). Field and Select use native elements with the label tied to the control and errors wired through <code>aria-invalid</code> + <code>aria-describedby</code> + <code>role=\"alert\"</code> (§1.3). Dialog is a true modal (<code>role=\"dialog\"</code>, <code>aria-modal=\"true\"</code>): it traps Tab focus within the panel, closes on Escape or backdrop click, and restores focus to the opener on close (§1.3). Because the primitives emit identical markup to the current hand-written classes, adopting them at existing call sites is a drop-in with no visual change - adoption is left incremental so this run touches no conversion-critical markup unattended. <code>tests/ui-primitives.test.ts</code> renders each and runs axe (no serious/critical). Verified: tsc clean, vitest 52/52, lint clean, next build rc=0.</p>";
