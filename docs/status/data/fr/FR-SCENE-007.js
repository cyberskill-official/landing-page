window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>One map MUST turn section progress into scene state.</p>\n<ol><li>Each narrative section MUST expose a normalized 0..1 progress value derived from the shared scroll loop in FR-SCENE-002.</li><li>A single progress map MUST translate those values into camera moves, model state, and lighting, so all scene consumers read one source of truth.</li><li>The map MUST be declarative data (section to targets), not logic scattered across components, and MUST clamp inputs to the 0..1 range.</li><li>When motion is not allowed the map MUST resolve to static end-state values so the scene still composes correctly without animation.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Each section reports a clamped 0..1 progress as the user scrolls.</li><li>Camera, model, and lighting all read from the one progress map.</li><li>With reduced motion, the map yields stable static values.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>lib/scene/progressMap.ts</code> is the single declarative map: keyframe stops (<code>at</code> -&gt; camera/model/light targets) as data, with <code>resolveSceneState(progress)</code> lerping between the surrounding stops and <code>clamp01</code> clamping input to 0..1 (clauses 1, 3). Both scene consumers read from it and nothing computes its own choreography: the <code>CameraRig</code> in <code>GenieScene</code> eases the camera toward <code>resolveSceneState(p).camera</code>, and <code>LumiPlaceholder</code> drives its spin/drift/glow from <code>.model</code> and the point light from <code>.light</code> (clause 2). <code>staticSceneState()</code> returns the opening composition for the reduced-motion / non-WebGL path, which the StaticPoster mirrors (clause 4). <code>tests/scene-progress-map.test.ts</code> covers clamping, end-state resolution, monotonic interpolation, and the static state. Verified by tsc + lint + 36 vitest tests + <code>next build</code> (rc=0).</p>\n<p>Note on clause 1: progress is currently the global story timeline (the shared 0..1 scroll loop); per-section sub-ranges can layer onto the same map when the pinned-section work (FR-SCENE-004) lands.</p>";
