window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The site SHOULD publish a statement that documents its accessibility conformance and explains the motion decision.</p>\n<ol><li>An <code>/[lang]/accessibility</code> page SHOULD state the target conformance level, known limitations, and a contact route for accessibility feedback.</li><li>The page MUST describe the always-on motion stance and point to the reduced-motion path and any user-facing motion control.</li><li>The statement MUST exist for both locales and MUST read in each locale's language.</li><li>The page MAY be marked <code>noindex</code> while it remains a placeholder, but its content MUST stay reachable through site navigation.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li><code>/en/accessibility</code> and <code>/vi/accessibility</code> resolve and state a conformance target plus a feedback contact.</li><li>The page explains the motion stance and links the reduced-motion path.</li><li>Each locale's statement reads in its own language.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>app/[lang]/accessibility/page.tsx</code> publishes the statement in both locales (<code>Record&lt;Locale, ...&gt;</code>): it names the WCAG 2.2 AA target, lists what has been done and the known limitations, and gives an email feedback route with a one-business-day aim. A dedicated \"Motion and the 3D scene\" block explains the always-on motion stance, and the page links the reduced-motion path with a real link to <code>/[lang]/lite</code>. The footer carries a standing link to the page, so it is reachable through navigation (clause 4). <code>generateMetadata</code> sets the canonical and en/vi hreflang alternates. Verified by <code>next build</code> (<code>/en/accessibility</code> and <code>/vi/accessibility</code> prerendered) and tsc + lint green.</p>";
