window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-006"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Screen readers SHOULD be told when chat content changes so streamed replies are not silent.</p>\n<ol><li>The chat transcript SHOULD sit in an ARIA live region so new and streaming messages are announced as they arrive.</li><li>The live region MUST use a politeness level that announces updates without interrupting the user mid-action.</li><li>Streaming text SHOULD be announced in coherent chunks, not character by character, to avoid a flood of fragments.</li><li>A busy or sending state MUST be exposed to assistive technology so users know a reply is in progress.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A new chat message is announced by a screen reader without moving focus.</li><li>Streaming output is announced in readable chunks, not per character.</li><li>The sending state is conveyed to assistive technology.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22 in <code>components/genie/GenieChatPanel.tsx</code>. The transcript container is <code>role=\"log\"</code> with <code>aria-live=\"polite\"</code> and <code>aria-relevant=\"additions text\"</code>, so new and streaming messages are announced as they arrive without moving focus (clauses 1-2). Streamed tokens are appended per network chunk (multi-character), which a polite live region coalesces into readable additions rather than character-by-character fragments (clause 3). The log carries <code>aria-busy={busy}</code>, which is true while the reply is thinking or streaming, exposing the sending state to assistive tech (clause 4). As an added safeguard the panel returns focus to the launcher on close (FR-A11Y-006 sibling focus concern). Verified by tsc + lint + <code>next build</code> green; manual screen-reader confirmation remains a recommended follow-up.</p>";
