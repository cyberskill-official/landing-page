window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>CI MUST be the machine that enforces what humans cannot eyeball.</p>\n<ol><li>CI MUST run the static verification script, a typecheck, and a build on every push and pull request, and MUST fail the job if any step fails.</li><li>CI MUST enforce a Lighthouse performance budget and MUST fail if the LCP budget regresses past 2500 ms.</li><li><code>lighthouse/budget.json</code> MUST be the machine-checked performance contract (script bundle, total bytes, and timing budgets), version-controlled so a regression is a diff.</li><li><code>scripts/verify-static.mjs</code> MUST be runnable locally so the same checks gate before a push.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A type error or failed build fails CI.</li><li>An LCP budget over 2500 ms in <code>budget.json</code> fails CI.</li><li><code>node scripts/verify-static.mjs</code> runs the static checks locally.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>.github/workflows/ci.yml</code> runs verify + typecheck + build + budget; <code>lighthouse/budget.json</code> sets the LCP ceiling; <code>scripts/verify-static.mjs</code> present. Deferred: a live CI run on the operator's GitHub.</p>";
