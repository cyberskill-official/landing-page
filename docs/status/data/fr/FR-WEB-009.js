window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-009"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Images SHOULD be served responsively so they help, not hurt, the largest contentful paint and the layout stability of the page.</p>\n<ol><li>Content images SHOULD render through <code>next/image</code> with explicit <code>sizes</code> so the browser fetches an appropriately sized source per viewport.</li><li>The hero or any above-the-fold image that is the LCP candidate MUST be marked <code>priority</code>, and below-the-fold images MUST NOT preempt it.</li><li>Images MUST reserve their intrinsic dimensions so loading them causes no layout shift.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Content images request a viewport-appropriate source via <code>sizes</code>, and the LCP image is prioritized.</li><li>No image load shifts surrounding layout.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. The first real content image - a licensed Adobe Stock aurora (golden particle wave, on-brand Umber/Ochre), resized to 2000px and committed at <code>public/brand/aurora-gold.jpg</code> - is rendered through <code>next/image</code> as the contact section backdrop. It uses a static import (so Next emits intrinsic dimensions and a blur placeholder), explicit <code>sizes=\"100vw\"</code>, and <code>fill</code> inside an absolutely positioned <code>inset:0</code> container, so it reserves its space and causes no layout shift (clauses 1, 3). It is below the fold and deliberately not <code>priority</code>, so it never preempts the LCP - which on this site is the server-rendered hero H1, not an image, so there is no above-the-fold LCP image to prioritize (clause 2). It shows only in dark theme (0.28 opacity) and is hidden in light theme to avoid muddying it. Verified by <code>next build</code> (rc=0, image optimizer active) plus tsc + lint + 37 vitest tests. Work-card and per-service images can adopt the same pattern when those assets exist.</p>";
