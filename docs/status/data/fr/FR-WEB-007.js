window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Every route SHOULD degrade gracefully, showing a considered loading and error state rather than a blank screen or an unhandled crash.</p>\n<ol><li>Routes SHOULD provide a <code>loading.tsx</code> boundary that renders a stable placeholder while a segment streams in.</li><li>Routes SHOULD provide an <code>error.tsx</code> boundary that catches a render failure, shows a recoverable message, and offers a retry.</li><li>Both boundaries MUST honor the active locale and the design tokens, and the error boundary MUST NOT leak internal error detail to the reader.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A slow segment shows the loading placeholder, then the content, with no blank frame.</li><li>A thrown render error is caught by the boundary and offers retry instead of crashing the route.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>app/[lang]/loading.tsx</code> renders a locale-neutral skeleton (<code>aria-busy</code>, visually-hidden \"Loading\", shimmer that honors <code>prefers-reduced-motion</code>) while a segment streams. <code>app/error.tsx</code> is the root error boundary: it catches render failures anywhere below the root layout (including the <code>[lang]</code> subtree), shows a recoverable message with a retry, and does not surface internal error detail. Skeleton styles live in <code>globals.css</code> (<code>.cs-skeleton*</code>). Verified by <code>next build</code> (26/26 static pages generated, the <code>[lang]</code> segment compiles with the loading boundary) and a clean tsc + lint run.</p>";
