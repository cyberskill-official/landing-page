window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-010"]="<h1>FR-OPS-010: Alert when every lead sink fails, and prove the pipeline weekly with a synthetic lead</h1>\n<h2>0. Why (evidence)</h2>\n<p>app/api/lead/route.ts fans out best-effort to four sinks (JSONL file, Resend, Slack, CyberOS webhook) and always returns ok:true to the visitor. The file sink always fails on Vercel's read-only filesystem and the other three no-op until their env vars exist - so a fully dead lead pipeline is invisible today. Every conversion FR in this backlog is worthless if the leads it wins land in a black hole.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 After the fan-out, the route SHALL count rejected sinks against configured sinks (a sink with no env var is skipped, not failed).</li><li>1.2 When every configured sink fails, or when zero sinks are configured in production, the route SHALL report the failure to the error tracker (FR-OPS-006) with the per-sink reasons, while keeping the ok:true response to the visitor.</li><li>1.3 A scheduled CI job SHALL POST a synthetic lead to production weekly; a synthetic lead SHALL skip the CRM forward and be tagged so it cannot pollute the real pipeline.</li><li>1.4 A failing synthetic run SHALL notify the owner.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 - a skipped (unconfigured) sink is not counted as a failure - test: <code>lead/sink-accounting</code></li><li>[x] AC for 1.2 - with all sinks mocked to reject, the tracker is called and the response stays ok:true - test: <code>lead/total-failure-alert</code></li><li>[x] AC for 1.3 - zero configured sinks in production triggers the alert - test: <code>lead/total-failure-alert</code></li><li>[x] AC for 1.4 - a synthetic lead skips the CRM forward and carries the tag - test: <code>lead/synthetic-path</code></li><li>[x] AC for 1.5 - the weekly workflow exists and fails loudly on a non-2xx - test: <code>ci/synthetic-lead-workflow</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A visitor must never see an error because a sink is down.</li><li>The synthetic lead must not trigger the auto-acknowledgement email (FR-CTA-011) to a real address.</li><li>Rate limiting must not block the weekly synthetic POST.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Deploying the CyberOS intake (FR-BIZ-002).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>A submitted lead is never silently lost.</li><li>AGENTS.md §4.2: the Anthropic key lives only in server env; no NEXT_PUBLIC_ secret, ever.</li></ul>";
