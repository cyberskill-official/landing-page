window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PERF-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The performance budget MUST be a machine-checked contract, not a hope.</p>\n<ol><li><code>lighthouse/budget.json</code> MUST declare a timing budget with an LCP ceiling of 2500 ms and byte ceilings for the script bundle and the total transfer.</li><li>CI MUST read that budget and MUST fail the job when any timing or byte ceiling regresses past its declared value.</li><li>The budget file MUST be version-controlled so every change to a ceiling lands as a reviewable diff.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li><code>lighthouse/budget.json</code> sets LCP to 2500 ms plus script and total byte caps.</li><li>A run that exceeds the LCP ceiling fails CI.</li><li>A bundle over the script byte cap fails CI.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped, verified, live.</p>";
