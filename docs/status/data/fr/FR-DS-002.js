window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The reader MUST be able to choose light or dark, and that choice MUST survive a reload without a flash of the wrong theme.</p>\n<ol><li>A header control MUST flip <code>data-theme</code> on <code>&lt;html&gt;</code> between <code>light</code> and <code>dark</code>, and the dark palette MUST come from the existing <code>[data-theme=\"dark\"]</code> tokens (no new colors invented here).</li><li>The chosen theme MUST be persisted to <code>localStorage</code>.</li><li>The saved theme MUST be applied before first paint via an inline script in the root layout, so there is no flash of the unsaved theme.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Toggling switches <code>data-theme</code> and re-renders in the other palette at once.</li><li>The choice persists across reload.</li><li>A dark-saved page paints dark on first frame (no light flash), and the control exposes <code>a11y.themeToDark</code> / <code>a11y.themeToLight</code> labels in both locales.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>components/header/ThemeToggle.tsx</code> flips and persists <code>data-theme</code>; the no-flash script in <code>app/layout.tsx</code> sets it pre-paint; dark tokens already exist in <code>app/globals.css</code>; labels added to <code>lib/i18n/dictionaries.ts</code>. Deferred: visual flash check and theme persistence on the operator machine.</p>";
