window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CTA-009"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The funnel MUST expose where leads drop off, so form friction can be found and fixed.</p>\n<ol><li>The form MUST emit a form-start event on first field interaction and a form-submit event on success, through the analytics layer from FR-OPS-002.</li><li>An abandonment event MUST fire when a started form is left without submitting, and SHOULD record the last field reached.</li><li>No event may carry raw personal data (email, message body) or any secret; only field names and coarse state may be sent.</li><li>Events MUST respect the consent state; tracking MUST NOT run before consent is given.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Starting then leaving the form produces start and abandonment events.</li><li>No event payload contains the lead's email or message text.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22 in <code>components/cta/LeadForm.tsx</code> via the FR-OPS-002 analytics layer. <code>form_start</code> fires once on first field focus (form <code>onFocus</code> + a one-shot ref); <code>lead_submitted</code> fires on a successful POST; <code>lead_abandoned</code> fires from a <code>pagehide</code> listener and on unmount when the form was started but not submitted, de-duplicated by a ref so it sends at most once (clauses 1-2). Both new names are added to the <code>AnalyticsEvent</code> union and the <code>/api/analytics</code> allowlist. Every payload carries only <code>{ source }</code> and the event name, never the email or message body (clause 3). Verified: tsc clean, vitest 21/21, next lint clean, build green.</p>\n<p>Two honest notes for the operator to confirm:</p>\n<ul><li>Clause 2 \"last field reached\" is a SHOULD and is not yet recorded; only start/submit/abandon state is sent.</li><li>Clause 4 (consent): these events are cookieless and carry no personal data, matching the anonymous first-party analytics described in the privacy notice, so they are treated as not requiring the lead-data consent checkbox (which governs processing of submitted contact details). If you want analytics gated behind an explicit tracking consent, that is a separate decision to make.</li></ul>";
