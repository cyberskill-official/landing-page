window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PERF-006"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Real-user metrics MUST be observed, not just lab scores.</p>\n<ol><li>The app MUST collect field Core Web Vitals from real visitors through Vercel Speed Insights, distinct from the synthetic Lighthouse runs.</li><li>The collected metrics MUST be visible in the project dashboard so LCP, CLS, and INP trends can be tracked over real traffic.</li><li>The integration MUST follow the dependency note in FR-OPS-007 so the <code>@vercel/*</code> peer conflict does not break installs.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Field CWV data appears in the Vercel dashboard from real sessions.</li><li>LCP, CLS, and INP are tracked over time.</li><li>Install succeeds with the documented peer-dependency handling.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped (integration). <code>&lt;SpeedInsights /&gt;</code> from <code>@vercel/speed-insights</code> is mounted in <code>app/layout.tsx</code>, so real-user LCP, CLS, and INP are collected from every route, distinct from the synthetic Lighthouse-CI runs (FR-PERF-002). This is live in production. The dashboard trend (§2 item 1-2) accrues with real traffic and is viewed in the Vercel project's Speed Insights tab - that part is data accumulation on Vercel's side, not a code deliverable. Install handling per FR-OPS-007 (no <code>.npmrc</code> needed; the peer conflict no longer reproduces). Verified: build green with the component mounted.</p>";
