window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<ol><li>MUST route every page under <code>/[lang]</code> with <code>lang in {en, vi}</code> and <code>generateStaticParams</code> for both; an invalid locale MUST <code>notFound()</code>.</li><li>The root layout MUST own <code>&lt;html&gt;</code>/<code>&lt;body&gt;</code> and MUST set <code>&lt;html lang&gt;</code> to the active locale (BCP-47), supplied by middleware via the <code>x-cs-locale</code> request header. Bare <code>/</code> MUST redirect to the default locale.</li><li>The locale layout MUST mount the shared chrome (skip link, header, footer, persistent CTA, Genie, canvas) and MUST emit per-locale metadata with hreflang alternates (<code>en</code>, <code>vi</code>, <code>x-default</code>).</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li><code>/en</code> and <code>/vi</code> resolve; <code>&lt;html lang&gt;</code> matches; <code>/</code> redirects to <code>/en</code>.</li><li><code>/zz</code> (invalid) returns 404.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: routes + middleware + generateStaticParams authored; <code>@/</code> imports resolve (verify script). Deferred: <code>next build</code> + route smoke test (operator).</p>";
