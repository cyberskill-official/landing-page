window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The home story MUST be server-rendered DOM, not painted by the canvas, so that every meaningful state is crawlable and survives with JavaScript disabled.</p>\n<ol><li>The home route MUST render hero, value proposition, services, work preview, social proof, careers, and contact as server components reading copy from the content source (FR-CMS-001) for the active locale.</li><li>The hero H1 MUST be the slogan \"Turn Your Will Into Real\" rendered in the SSR DOM, and MUST be the LCP element. The 3D canvas MUST NOT own LCP and MUST NOT be required for any text to be present.</li><li>Reveal animations MUST be opacity-only (no layout shift) and MUST be motion- safe: under <code>prefers-reduced-motion: reduce</code> the <code>Reveal</code> wrapper MUST render its children fully visible with no transform.</li><li><code>/work</code> and <code>/careers</code> MUST exist as their own crawlable routes per locale.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>View-source on <code>/en</code> and <code>/vi</code> shows the slogan H1 and all seven section headings as text before any client script runs.</li><li>With motion reduced, content is visible immediately (no opacity:0 trap).</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: seven section components + <code>Reveal</code> + three routes authored; copy is read from <code>lib/content/site.ts</code>; <code>Reveal</code> branches on reduced motion. Deferred: <code>next build</code> render and LCP-element confirmation on the operator machine.</p>";
