window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>There MUST be one fixed canvas that fails closed and never owns LCP.</p>\n<ol><li>The app MUST mount a single fixed, full-viewport R3F canvas so 3D assets load once and persist across scroll.</li><li>The scene MUST be a dynamic import with <code>ssr: false</code> that fails closed: any load or capability failure MUST leave the static poster in place.</li><li>The canvas MUST mount only when all hold: desktop, fine pointer, at least four logical cores, and motion allowed. Otherwise it MUST NOT mount.</li><li>A static CSS poster MUST be the SSR and initial paint, and the mobile / low- GPU / reduced-motion fallback.</li><li>The canvas MUST use an alpha (transparent) background so the SSR hero shows through, and MUST NOT become the LCP element.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>On mobile / reduced motion, only the static poster renders; no WebGL context.</li><li>On a capable desktop, the canvas mounts over the still-present SSR hero.</li><li>A forced scene-import failure leaves the poster and breaks nothing.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>CanvasMount</code> gates on pointer/cores/motion/desktop and dynamically imports <code>GenieScene</code> with <code>ssr:false</code>; <code>StaticPoster</code> is the fallback; canvas is alpha. Deferred: GPU behaviour and LCP confirmation on the operator machine.</p>";
