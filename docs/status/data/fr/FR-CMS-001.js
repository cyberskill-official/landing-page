window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CMS-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Content MUST be defined once and serve both the page and the Genie, so the two can never disagree on a fact.</p>\n<ol><li>Every user-facing string MUST ship an English and a Vietnamese value; a key present in one locale MUST be present in the other.</li><li>Company facts (services, Ho Chi Minh City base, established 2020, contact details, slogan) MUST live in one content module and MUST feed both the rendered sections and the Genie persona grounding (FR-CHAR-011).</li><li>The content shape MUST be typed so a missing field is a build-time error, not a runtime blank.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Both <code>en</code> and <code>vi</code> resolve a value for every content key.</li><li>The contact facts shown on the page are the same objects the persona reads.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>lib/content/site.ts</code> and <code>lib/i18n/dictionaries.ts</code> carry both locales; the persona imports the shared facts. Deferred: end-to-end copy review and native-Vietnamese proofread (operator / Stephen).</p>";
