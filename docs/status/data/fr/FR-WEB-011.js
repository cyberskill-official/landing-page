window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-011"]="<h1>FR-WEB-011: Serve the negotiated locale at the root without a visible redirect hop</h1>\n<h2>0. Why (evidence)</h2>\n<p>Audit B: cyberskill.world/ returns a 307 to /en and Lighthouse measured ~0.84 s of mobile delay from that hop ahead of any content - a full round trip on every first visit. middleware.ts currently redirects \"/\" after negotiating the locale (FR-WEB-004).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 A request to <code>/</code> SHALL return the negotiated locale's page in the first response (an edge rewrite, not a redirect), with HTTP 200.</li><li>1.2 The response SHALL carry the correct <code>&lt;html lang&gt;</code>, canonical and hreflang for the served locale, and <code>/</code> SHALL canonicalise to the locale URL so no duplicate content is indexed.</li><li>1.3 The cs-locale cookie SHALL keep priority over Accept-Language negotiation (the FR-WEB-004 contract is preserved).</li><li>1.4 The measured navigation to <code>/</code> on mobile SHALL show no redirect entry in the resource timeline.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 - GET / returns 200 with rendered content - test: <code>routing/root-no-redirect</code></li><li>[x] AC for 1.2 - the served page's canonical points at /en or /vi and hreflang stays reciprocal - test: <code>seo/canonical-hreflang</code></li><li>[x] AC for 1.3 - a cs-locale=vi cookie serves the VN page at / - test: <code>routing/root-cookie-priority</code></li><li>[x] AC for 1.4 - zero redirects in the navigation timeline - test: <code>perf/no-root-redirect</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Bots with no Accept-Language must get the x-default (EN) without a redirect chain.</li><li>Caching: the rewrite must vary correctly so a VN visitor never gets a cached EN page.</li><li>Sitemap and internal links keep pointing at /en and /vi, never at bare /.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Changing the locale negotiation rules (FR-WEB-004).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>Every locale stays a crawlable URL with reciprocal hreflang (AGENTS.md §4.5).</li><li>AGENTS.md §4.1 HTML-first: every meaningful state stays server-rendered DOM and the canvas never owns LCP.</li></ul>";
