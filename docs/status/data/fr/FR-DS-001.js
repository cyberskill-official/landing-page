window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-001"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The system MUST express the doctrine anchors as CSS custom properties and MUST NOT hardcode brand hex in components.</p>\n<ol><li>MUST define <code>--cs-color-brand-umber</code> (#45210E) and <code>--cs-color-brand-ochre</code> (#F4BA17), with a <code>color(display-p3 ...)</code> upgrade under <code>@supports</code>.</li><li>MUST ship the Liquid Glass five materials (whisper/light/standard/heavy/solid) as <code>--cs-glass-*</code> plus <code>.cs-surface-*</code> classes, and MUST collapse them to solid under <code>prefers-reduced-transparency</code>, <code>forced-colors</code>, <code>@supports not (backdrop-filter)</code>, and <code>@media print</code>.</li><li>MUST provide type, space, radius, depth, and motion scales as tokens.</li><li>MUST support <code>data-theme=\"light|dark\"</code> (light default).</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Components reference only <code>--cs-*</code> for brand colour (grep shows no raw <code>#45210E</code> outside <code>globals.css</code> and the favicon).</li><li>Glass surfaces render solid when transparency is unsupported/unwanted.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: token block present in <code>app/globals.css</code>; fallback media queries present. Deferred: visual APCA Lc measurement on rendered glass (operator).</p>";
