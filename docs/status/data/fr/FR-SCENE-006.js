window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-006"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Lumi's magic SHOULD be a custom shader, not a stock material.</p>\n<ol><li>A custom GLSL shader material MUST render Lumi's golden glow in the brand Ochre hue, driven by a time uniform for a slow living pulse.</li><li>The shader MUST support a dissolve or particle effect that can be triggered on appearance and dismissal of the genie.</li><li>Glow intensity and particle density MUST expose uniforms so other systems can drive them from scene progress or chat state.</li><li>The shader MUST degrade to a plain emissive material when WebGL features are unavailable or motion is not allowed.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>Lumi shows a golden Ochre glow that pulses over time.</li><li>Appearance and dismissal play a dissolve or particle transition.</li><li>On a reduced or unsupported path, a plain emissive fallback renders.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>LumiPlaceholder</code> now wraps the lit gold core in a custom <code>THREE.ShaderMaterial</code> aura (additive, depth-write off): a fresnel edge glow in the brand Ochre (<code>uCore</code> #F4BA17 -&gt; <code>uRim</code> #FFE7A6) with a <code>uTime</code>-driven shimmer for a slow living pulse (clause 1). A <code>uReveal</code> uniform gates the alpha through a moving shimmer band, so Lumi dissolves into being on appearance (clause 2); the uniform is exposed so a dismissal can drive it back down. Glow intensity is uniform-driven from scene progress (<code>uProgress</code> &lt;- the progress map's glow) and chat state (<code>uPulse</code> &lt;- idle/thinking/speaking), so other systems steer it (clause 3). The non-WebGL / incapable path renders the StaticPoster, and the lit <code>MeshStandardMaterial</code> core is the plain emissive fallback if the shader is absent (clause 4). Verified by <code>next build</code> (rc=0, scene chunk compiles) with no runtime WebGL/shader console errors on the running dev server; tsc + lint + 36 vitest tests green. Note: GLSL is runtime-compiled, so the build proves type/compile health, not pixels - a visual pass on the deploy is the final check.</p>";
