window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PERF-010"]="<h1>FR-PERF-010: Serve brand image assets with long immutable caching</h1>\n<h2>0. Why (evidence)</h2>\n<p>Audit B: static build assets carry immutable one-year caching, but logo.svg, lumi-poster.webp and favicon.svg are served with max-age 0, so they revalidate on every visit - repeat-visit round trips for assets that never change.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Files under public/ that are content-stable (logo.svg, favicon.svg, lumi-poster.webp, brand/, models/) SHALL be served with <code>Cache-Control: public, max-age=31536000, immutable</code>, added in next.config.ts headers().</li><li>1.2 Any such asset that can change without a filename change SHALL be fingerprinted (or moved into the build pipeline) before the immutable header is applied.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - a HEAD request to each listed asset returns the immutable cache header - test: <code>headers/static-asset-cache</code></li><li>[ ] AC for 1.2 - no non-fingerprinted, editable asset carries <code>immutable</code> - test: <code>headers/static-asset-cache</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The GLB model files under public/models are large - immutable caching must not defeat a future model swap; fingerprint them.</li><li>Vercel may already set headers for some paths; the config must not conflict.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<ul><li>Image optimisation itself (next/image already handles it).</li></ul>\n<h2>5. Protected invariants this FR must not weaken</h2>\n<ul><li>AGENTS.md §4.7: the CI performance budget (lighthouse/budget.json) is never relaxed to make a gate green.</li></ul>";
