window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-005"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Anything the canvas communicates SHOULD also exist as readable DOM text so it is available without WebGL.</p>\n<ol><li>Every meaningful message conveyed by the canvas SHOULD have an equivalent in the DOM, reachable by assistive technology.</li><li>A <code>&lt;noscript&gt;</code> fallback MUST present that text when scripts or WebGL do not run, so no information depends solely on the scene.</li><li>The canvas element MUST carry an appropriate accessible name or be marked decorative when its content is mirrored elsewhere.</li><li>The mirrored text MUST stay consistent with the story the canvas tells and MUST be localised per route.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>With scripts disabled, the <code>&lt;noscript&gt;</code> text conveys the canvas message.</li><li>A screen reader can reach the mirrored text in the DOM.</li><li>The canvas has an accessible name or is correctly marked decorative.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped. The canvas layer (<code>.cs-canvas-layer</code>) is <code>aria-hidden</code> and pointer-events:none, so it is correctly marked decorative (§1.3). All meaningful content (hero, sections, contact) is server-rendered HTML, so nothing is canvas-only (§1.1). Lumi's communicative states are now mirrored as DOM text by <code>components/genie/GenieStatusAnnouncer.tsx</code>: a <code>.cs-visually-hidden</code> <code>role=\"status\" aria-live=\"polite\"</code> region that announces \"Lumi is thinking\" / \"Lumi is responding\" from the genie store, localised per route (§1.1, §1.4). A <code>&lt;noscript&gt;</code> note in the locale layout describes the decorative scene and points to the on-page text/controls when scripts or WebGL do not run (§1.2), localised en/vi. Verified: tsc clean, vitest 44/44, next build rc=0.</p>";
