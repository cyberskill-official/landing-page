window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-A11Y-007"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Surfaces SHOULD stay legible and operable when the user forces a high-contrast colour scheme.</p>\n<ol><li>Under <code>forced-colors: active</code>, all text, controls, and focus indicators MUST remain visible and use the system colour keywords rather than fixed brand colours.</li><li>Glass and translucent surfaces MUST fall back to opaque, bordered backgrounds so content does not wash out against the forced palette.</li><li>Meaning carried by colour alone MUST keep a non-colour cue, such as a border, icon, or text label, in forced-colors mode.</li><li>Decorative gradients and shadows MAY be dropped, but no interactive element MUST disappear or lose its boundary.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>In a forced-colors mode, text, controls, and focus rings stay visible.</li><li>Glass surfaces render as opaque, bordered panels.</li><li>No interactive element loses its visible boundary.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. The <code>@media (forced-colors: active)</code> block in <code>globals.css</code> now: maps the keyboard focus ring to <code>Highlight</code>; keeps every glass/translucent surface opaque with a <code>CanvasText</code> border and no backdrop-filter (clause 2); gives buttons, the theme toggle, and the chat close a <code>ButtonText</code> border (with <code>forced-color-adjust: none</code> on the primary button so it stays a filled control); borders form fields, the chat input, chat bubbles, the persistent CTA, tags, and process steps so nothing relies on colour alone (clauses 1, 3, 4). All values use CSS system-colour keywords, so the OS palette drives the result. Verified by <code>next build</code> (rc=0); the rules are scoped to the forced-colors media query and do not affect the default themes.</p>";
