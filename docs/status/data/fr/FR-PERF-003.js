window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PERF-003"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Heavy assets MUST not creep past their budget unnoticed.</p>\n<ol><li><code>perf/asset-budget.json</code> MUST declare per-category byte ceilings for the GLB model, textures, and the JavaScript bundle.</li><li><code>scripts/check-asset-size.mjs</code> MUST measure the built assets against those ceilings and MUST exit non-zero when any category regresses.</li><li>CI MUST run the guard on every build so a regression fails the job rather than shipping.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A GLB or texture over its ceiling fails the build.</li><li>A JS bundle over its ceiling fails the build.</li><li><code>node scripts/check-asset-size.mjs</code> runs the same check locally.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped. <code>scripts/asset-budget.json</code> declares the ceilings (maxImageKB 200, maxGlbKB 4096, maxPublicTotalKB 600, maxClientJsTotalKB 2800). <code>scripts/check-asset-size.mjs</code> walks <code>public/</code> (per-image and per-GLB caps + total) and the built client JS in <code>.next/static/chunks</code> (total), printing the measured sizes and exiting non-zero on any breach. It is wired as <code>npm run check:assets</code> and runs in the CI <code>build</code> job right after <code>next build</code>, so a regression fails the job before deploy. Runs locally with the same command.</p>\n<p>Current measured: public 95KB (largest asset aurora 95KB), client JS 2286KB across 43 chunks - both within budget. When the commissioned Lumi GLB lands (FR-CHAR-022), bump <code>maxGlbKB</code> after optimizing/Draco-compressing it. (Budget file lives at <code>scripts/asset-budget.json</code>, not <code>perf/</code>.)</p>";
