window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-003"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Sub-pages SHOULD declare their position in the site hierarchy so search engines can render breadcrumb trails and understand structure.</p>\n<ol><li>Each sub-page under <code>/work</code>, <code>/careers</code>, <code>/services</code>, and case studies SHOULD embed <code>BreadcrumbList</code> JSON-LD describing the path from home to the current page.</li><li>The breadcrumb items MUST use absolute URLs and MUST match the visible navigation order and labels on the page.</li><li>The home page MUST be the first item in every trail, and the current page MUST be the last item without an outgoing link.</li><li>Breadcrumb names SHOULD honour the active locale so <code>en</code> and <code>vi</code> trails read in their own language.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A case-study page source contains valid <code>application/ld+json</code> of type <code>BreadcrumbList</code> with home as the first item.</li><li>Breadcrumb item URLs are absolute and resolve to real routes.</li><li>Trails on <code>vi</code> pages use Vietnamese labels.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>components/seo/BreadcrumbJsonLd.tsx</code> is a server component that emits a <code>BreadcrumbList</code> with <code>ListItem</code> positions, absolute <code>item</code> URLs built from <code>NEXT_PUBLIC_SITE_URL</code> (falling back to <code>company.url</code>, https://cyberskill.world), and locale-aware names. It is rendered on <code>work/[slug]</code> (Home &gt; Work &gt; title), <code>work</code> (Home &gt; Work), <code>careers</code> (Home &gt; Careers), <code>privacy</code>, and <code>accessibility</code>. Home is always the first item and the current page is the last. <code>vi</code> trails use Vietnamese labels (\"Trang chủ\", \"Dự án\", \"Tuyển dụng\"). Verified by <code>next build</code> (sub-pages prerendered) and tsc + lint green.</p>";
