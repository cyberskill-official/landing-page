window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CTA-002"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>A path to convert MUST be available at all times and MUST NOT depend on the 3D scene having loaded or finished.</p>\n<ol><li>A persistent CTA MUST render in the shared chrome on every page and MUST stay reachable regardless of scroll position or scene state.</li><li>The Genie-open control MUST trigger the chat path directly and MUST NOT wait on the canvas; if the scene never mounts, the button MUST still work.</li><li>The persistent CTA MUST be hidden from print output (<code>@media print</code>).</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>The CTA and Genie-open button are present on <code>/en</code>, <code>/vi</code>, <code>/work</code>, and <code>/careers</code> without scrolling into a section.</li><li>Disabling the canvas leaves both controls fully operable.</li><li>Print preview omits the persistent CTA.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Static: <code>PersistentCta</code> mounted in the locale layout chrome; <code>GenieOpenButton</code> dispatches to the Genie state, not the scene; print-hidden style present. Deferred: visual print-preview check on the operator machine.</p>";
