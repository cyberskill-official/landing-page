window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OPS-017"]="<h1>FR-OPS-017: Expose static CDN Cache-Control headers on sitemaps, feeds, and LLM text routes</h1>\n<h2>0. Why (evidence)</h2>\n<p>To optimize serverless execution costs and prevent high-frequency crawler queries on static xml/txt routes from exhausting computing resources. Setting explicit cache-control directives allows edge CDNs to cache responses safely and invalidate them gracefully.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 All dynamic XML sitemaps and RSS feeds SHALL return a <code>Cache-Control</code> header allowing public caching for at least 24 hours.</li><li>1.2 The AI helper text endpoints (<code>/llms.txt</code> and <code>/llms-full.txt</code>) SHALL return Cache-Control headers allowing public caching.</li><li>1.3 The routes SHALL implement stale-while-revalidate patterns (<code>s-maxage=86400, stale-while-revalidate=3600</code>) so CDNs update cache in the background.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 - GET requests to sitemaps and feeds return Cache-Control with public s-maxage - test: <code>perf/xml-cache-headers</code></li><li>[ ] AC for 1.2 - GET requests to /llms.txt and /llms-full.txt return Cache-Control with public s-maxage - test: <code>perf/llms-cache-headers</code></li><li>[ ] AC for 1.3 - CDN caching behaves as stale-while-revalidate in live staging checks - test: <code>perf/cdn-cache-validation</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Deployments updating the sitemap, but CDNs serving stale content to search engines past the 24-hour window.</li><li>Private headers accidentally leaking into public cache spaces.</li></ul>\n<h2>4. Out of scope</h2>\n<ul><li>Caching policies for dynamic user API routes (<code>/api/lead</code> or <code>/api/genie</code>).</li></ul>\n<h2>5. Protected invariants</h2>\n<ul><li>Cached sitemaps and feeds must always match their language-specific alternates.</li></ul>";
