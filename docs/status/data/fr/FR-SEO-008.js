window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-008"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Each page SHOULD present a distinct social preview image so shared links look deliberate rather than generic.</p>\n<ol><li>Each route SHOULD generate a per-page OpenGraph image at 1200 by 630 pixels, including case studies and insights.</li><li>The image SHOULD render the page title and the CyberSkill mark using the brand palette and type, derived from page content.</li><li>Each page MUST set <code>og:image</code>, its width and height, and a matching <code>twitter:image</code> so previews are consistent across networks.</li><li>A safe default image MUST exist for routes that supply no specific title.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A case-study and an insight page each emit a unique 1200x630 OpenGraph image.</li><li>The image shows the page title and brand mark.</li><li><code>og:image</code> dimensions and <code>twitter:image</code> are present and correct.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>app/[lang]/work/[slug]/opengraph-image.tsx</code> generates a 1200x630 PNG per case study: it reads the slug, looks up the work item, and renders the localized case title plus the client and the CYBERSKILL mark on the Umber/Ochre brand palette, using system fonts so it builds offline. Next wires <code>og:image</code> (with width/height) and <code>twitter:image</code> automatically from the file convention. The existing <code>app/[lang]/opengraph-image.tsx</code> is the safe default for routes with no specific title (clause 4). Verified by <code>next build</code>, which registers both <code>/[lang]/opengraph-image</code> and <code>/[lang]/work/[slug]/opengraph-image</code>.</p>\n<p>Scope note: the insight OG image (clause 1) waits on the insights route, which does not exist yet; the same component pattern will apply when it ships.</p>";
