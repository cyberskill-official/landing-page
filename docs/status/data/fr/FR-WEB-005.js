window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-005"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Content pages SHOULD refresh without a full redeploy, so editors can publish a change and have it appear within a bounded window.</p>\n<ol><li>Content routes SHOULD set a <code>revalidate</code> interval so pages are regenerated in the background after they go stale.</li><li>The system SHOULD expose an on-demand revalidation route that re-renders a given path or tag when triggered.</li><li>The revalidation route MUST reject unauthenticated or unauthorized callers and MUST NOT revalidate arbitrary paths without a valid secret.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A content edit appears after the revalidate window without a redeploy.</li><li>Calling the revalidation route with a valid secret refreshes the target path; an invalid secret is rejected.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Not yet implemented; acceptance pending build.</p>";
