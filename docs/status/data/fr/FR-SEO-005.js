window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SEO-005"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Every route, including dynamic ones, SHOULD declare correct language alternates and a canonical so duplicate-locale signals stay clean.</p>\n<ol><li>Every static and dynamic route SHOULD emit <code>en</code> and <code>vi</code> hreflang alternates plus a single <code>x-default</code>, including <code>/work/[slug]</code> and <code>/insights/[slug]</code>.</li><li>Each page MUST declare exactly one self-referential canonical URL that matches the route it serves.</li><li>The <code>x-default</code> alternate MUST point to a stable default-locale URL and MUST be consistent across the whole site.</li><li>Alternates MUST be reciprocal: the <code>en</code> page MUST link its <code>vi</code> twin and vice versa, with no dangling or mismatched pairs.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A dynamic case-study and insight URL each expose <code>en</code>, <code>vi</code>, and <code>x-default</code> alternates.</li><li>Each route declares one self-referential canonical.</li><li>Spot-checked alternate pairs are reciprocal and resolve.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>lib/seo/metadata.ts</code> <code>pageMetadata()</code> returns one self-referential canonical (<code>/{locale}{path}</code>) and a complete, reciprocal hreflang set (<code>en</code>, <code>vi</code>, <code>x-default</code> -&gt; <code>/en{path}</code>) for any route. The <code>[lang]</code> layout already emits the same set for the home route, and the helper is now applied to work, work/[slug], careers, privacy, and accessibility, so static and dynamic routes alike expose <code>en</code>/<code>vi</code>/<code>x-default</code> and exactly one canonical. A vitest unit test (<code>tests/seo-metadata.test.ts</code>) asserts the canonical and the three-key language map for both a sub-path and the home path. Verified by <code>next build</code> (26/26 pages) plus tsc + lint green.</p>\n<p>Scope note: the insight route in clause 1/acceptance does not exist yet (insights content FRs are still planned); the same helper will cover <code>/insights/[slug]</code> when that route ships.</p>";
