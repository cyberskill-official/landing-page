window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SCENE-004"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>Pinned sections SHOULD hold in place while their content scrubs against scroll.</p>\n<ol><li>Selected sections MUST pin to the viewport via GSAP ScrollTrigger so the section stays fixed while the page scroll advances its internal timeline.</li><li>Pinned timelines MUST use <code>scrub</code> so progress maps to scroll position and reverses cleanly when the user scrolls back up.</li><li>Pinning MUST be disabled when motion is not allowed (see FR-A11Y-001); the section MUST then render as ordinary static content.</li><li>Pin spacing MUST NOT leave layout gaps or overlap adjacent sections, and MUST recompute on resize.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A pinned section holds while its content animates, then releases on schedule.</li><li>Scrolling up reverses the scrubbed timeline without jumps.</li><li>With reduced motion, the section is static and no pin is created.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Shipped 2026-06-22. <code>lib/scroll/lenis-gsap.ts</code> <code>initScrollStory()</code> now creates a GSAP ScrollTrigger that pins the hero (<code>.cs-hero</code>) with <code>start: \"top top\"</code>, <code>end: \"+=55%\"</code>, <code>pin: true</code>, <code>pinSpacing: true</code>, <code>scrub: true</code>: the hero holds while the first stretch of scroll scrubs the scene intro, then releases, and <code>scrub</code> makes it reverse cleanly on scroll-up (clauses 1-2). <code>pinSpacing</code> keeps the layout gap-free and ScrollTrigger refreshes its measurements on resize (clause 4); <code>destroy()</code> kills the trigger with the rest. The pin is created only on desktop (&gt;=1024px) with motion allowed - reduced motion or narrow viewports keep the ordinary static flow (clause 3). Lenis 1.3 drives native window scroll, so the ScrollTrigger default scroller matches and pinning composes correctly. Verified by tsc + lint + <code>next build</code> (rc=0, 26/26).</p>\n<p>Tuning note: the pin distance and feel are confirmed on the deploy, since the always-animating canvas blocks the dev screenshot. If the hold feels long or short, <code>end</code> is the one knob to adjust.</p>\n<h2>§4 Addendum 2026-07-02: hydration-race defect + fix (round 5)</h2>\n<p>Defect: <code>pin: true</code> wraps <code>.cs-hero</code> in a GSAP-owned <code>div.pin-spacer</code> - real DOM surgery. The pin was created from the locale-layout provider (ScrollStoryProvider) behind an async import, so it raced PAGE-segment hydration; when the round-5 bundle growth slowed hydration enough, GSAP won and mutated the DOM mid-hydration. React 19 threw error #418 (\"server HTML didn't match the client\": expected <code>section.cs-hero</code>, found <code>div.pin-spacer</code>), regenerated the entire client tree, and every one-shot IntersectionObserver in MotionExtras was left watching orphaned nodes - all [data-mask-reveal]/[data-pop] content (including the new FR-DS-014 kinetic headings) stayed permanently hidden. Diagnosed with a <code>next dev</code> run whose unminified hydration diff named the pin-spacer directly.</p>\n<p>Fix: pin creation moved out of the provider into <code>createHeroPin()</code> (lib/scroll/lenis-gsap.ts) called by <code>&lt;HeroPin/&gt;</code> - a null-rendering client component mounted LAST in the page segment (app/[lang]/page.tsx). A page component's effect cannot run before its own segment finished hydrating, so the DOM surgery is provably post-hydration; unmount kills the trigger (which also restores the original DOM), so locale switches recreate it cleanly. Defence in depth: the MotionExtras reveal observer is now self-healing (re-scans still-pending nodes at +1.5s, +4s, and window load), and the shots harness gained REVEAL_PROBE (kinetic h2 must be \"shown\" + unmasked after scroll) and HYDRATION_PROBE (zero #418/hydration pageerrors), so this class of regression can never pass silently again. Behaviour, guards (desktop-only, reduced-motion off), and the <code>end: +=55%</code> tuning are unchanged.</p>";
