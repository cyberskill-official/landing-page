window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DS-005"]="<h2>§1 Requirement (BCP-14 normative)</h2>\n<p>The team MUST settle, in writing, whether the published design-system packages can be consumed, because the rest of the DS work forks on that answer.</p>\n<ol><li>The investigation MUST determine whether <code>@cyberskill/tokens</code>, <code>@cyberskill/react</code>, and <code>@cyberskill/style-packs</code> are installable from a reachable private registry under the project's credentials.</li><li>If they install, the project SHOULD consume them and apply the CyberSkill theme on top; if they do not, the project MUST keep the hand-ported tokens (FR-DS-001) as the source of truth.</li><li>The outcome MUST be recorded as a dated decision note resolving the Phase 0 open question either way.</li></ol>\n<h2>§2 Acceptance</h2>\n<ul><li>A reproducible install attempt is documented with its result (success or the exact failure).</li><li>A decision note states the chosen path and updates the dependency on FR-DS-001.</li></ul>\n<h2>§3 Evidence</h2>\n<p>Not yet implemented; acceptance pending build.</p>";
