/**
 * Named re-export of the package Card panel.
 *
 * For glass storytelling, compose with package `.cs-surface-*` /
 * `.cs-glass-card` classes (see glass.css). Layout-specific surfaces
 * (work cards, header chrome, metric tiles) stay class-only.
 */
export {
  Card,
  CardHeader,
  CardBody,
  CardFooter,
} from "@cyberskill/design";
export type { CardProps } from "@cyberskill/design";
