/**
 * Named re-export of the package Icon.
 *
 * All nine Lumi icon names (`close`, `sun`, `moon`, `arrow-right`, `check`,
 * `sparkle`, `chat`, `sound-on`, `sound-off`) match the package set 1:1.
 * `components/ui/Icon.tsx` re-exports this for existing import paths.
 */
export { Icon } from "@cyberskill/design";
export type { IconProps, IconName } from "@cyberskill/design";
