"use client";

// Phase-3 placeholder. Replaced with the real React Three Fiber scene once the
// 3D dependencies are installed. Returning null keeps the Phase-1 build green
// without three/@react-three/* present (progressive enhancement).
export function GenieScene() {
  return null;
}
